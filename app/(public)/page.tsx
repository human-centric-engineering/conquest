// app:shim — Sunrise-tracked route file reduced to a re-export of fork-owned
// content (CUSTOMIZATION.md §6). The ConQuest homepage lives in
// components/app/marketing/conquest-home.tsx; keep this one line on upstream
// merges ("keep mine"). Replaces Sunrise's default landing page.
export { default, metadata } from '@/components/app/marketing/conquest-home';

// app:shim — fork-owned page content (CUSTOMIZATION.md §6) re-exported so the
// ConQuest waitlist lives under components/app/marketing. New ConQuest surface
// (no Sunrise equivalent), but kept consistent with the contact-page shim.
export { default, metadata } from '@/components/app/marketing/waitlist-page';

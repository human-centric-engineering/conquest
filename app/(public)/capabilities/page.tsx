// Fork-owned route. The page content lives in
// components/app/marketing/capabilities-page.tsx, matching how the contact and
// waitlist marketing pages are structured (content in components/app/marketing,
// route file kept to a re-export).
export { default, metadata } from '@/components/app/marketing/capabilities-page';

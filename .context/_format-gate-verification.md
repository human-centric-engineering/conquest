# Format-gate verification (throwaway)

This file exists only to verify that docs-only PRs now run the *format* check.
Prettier should flag the emphasis above and fail the `Lint & format` job, proving
the gate now covers `.context/**` markdown. This PR will be closed without merging.

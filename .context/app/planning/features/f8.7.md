---
feature: F8.7
title: Respondent profile capture — in-flow form gate, per-field validation, conversational mode
phase: P8 — Admin analytics, exports, anonymous mode
status: done
owner: TBD
deps: F8.3 (profile snapshot + anonymous contract), F7.2 (SessionWorkspace carousel), F6.1 (sessions)
opened: 2026-07-15
plan: .claude/plans/we-can-currently-use-prancy-noodle.md
docs: .context/app/questionnaire/profile-capture.md
---

# F8.7 — Respondent profile capture (form gate + validation + conversational)

> Committable tracker for **F8.7**. Extends F8.3's profile capture into a first-class feature: a
> **blocking in-flow form gate** (the default), an alternative **conversational** collection mode, and
> **per-field validation** (deterministic / agentic / hybrid). Gated by `APP_QUESTIONNAIRES_ENABLED`
> (no dedicated capture flag — purely per-version config).

## Intent

F8.3 collected `profileFields` via a **pre-session** form (`ProfileStartForm`) on the `/start` page,
authed-invitation path only — the public/no-login path never collected them. This feature makes
capture a real in-flow step and adds validation:

1. **Form gate (default).** A standard form rides the carousel **after the intro, before the chat**,
   blocking progress (and the opening LLM turn) until filled + validated.
2. **Conversational mode.** The interviewer gathers the fields naturally in-chat; a best-effort pass
   persists them once complete.
3. **Per-field validation.** Deterministic (Zod/regex), agentic (LLM tidy + garbage-flag), or hybrid.

## Decisions (confirmed with the user)

- **Both modes now; `captureMode` default `form`.** Conversational is the lighter, deferrable second
  way — built, but the form gate is the primary deliverable.
- **Gate keys off `anonymousMode`, NOT authed-vs-public.** A public no-login link CAN collect a name
  (admin's choice); an anonymous link stays PII-free with no gate/directive/snapshot. `resolveSessionCapture`
  owns this — moved OUT of `start-context.ts`. Invariant enforced in three places (resolver null,
  server PUT reject, workspace `showCapture` false); the PUT is the true boundary.
- **Reuse `profileFields`.** Extend `ProfileFieldConfig` with a per-field `validation` mode (in the
  existing JSON — no column); add scalar `captureMode` (one column, one migration).
- **Agentic validation is non-fatal.** An LLM outage falls back to the deterministic value — never
  blocks a respondent (mirrors `resolveAnswerFit`).
- **Supersede the pre-session form.** `ProfileStartForm` + `start-context.ts` deleted; the `/start`
  page just creates/resumes (idempotent) and redirects. `buildFormSchema` moved to `profile/form-schema.ts`.

## Build shape

- **Schema / types** — `types.ts`: `PROFILE_FIELD_VALIDATION_MODES`, `CAPTURE_MODES`,
  `ProfileFieldConfig.validation`, config `captureMode` (+ default). `config-schema.ts`: per-field
  `validation` (optional-with-default so legacy JSON parses) + `captureMode`. Prisma: `captureMode`
  column (migration `20260715120000_app_questionnaire_capture_mode`, hand-authored ADD COLUMN, phantom
  pgvector DROPs avoided; 5 vector indexes verified intact). `detail.ts`: `asCaptureMode` narrower +
  `asProfileFields` routed through `parseProfileFields` (applies the validation default).
- **Validation service** — `profile/validate-profile-fields.ts`: `validateProfileSubmission` composing
  deterministic/agentic/hybrid, batched `runStructuredCompletion` (chat tier), non-fatal, cost-logged.
- **Runtime endpoint** — `questionnaire-sessions/[id]/profile/route.ts` (GET config+satisfied, PUT
  validate+upsert), `resolveTurnAccess`, anonymous guard, `withLiveSessionsEnabled`. Shared
  `profile/profile-snapshot.ts` (idempotent upsert) + `profile/resolve-capture.ts` (`resolveSessionCapture`).
- **Carousel gate** — `session-workspace.tsx`: new blocking `capture` view (intro → capture → persona
  → chat), kickoff deferred until `captureDone`, forward-nav lock + toggle suppression while blocking.
  Surface `profile/profile-capture-gate.tsx` (RHF + Zod + `FieldHelp`, PUTs, maps `fieldErrors`).
- **Seeding** — authed `[sessionId]/page.tsx` SSR-resolves `capture`; `anonymous-session-boot.tsx`
  `fetchCapture` (fails soft to null).
- **Admin UI** — `config-editor.tsx`: "Respondent profile fields" group gains a `captureMode` control
  - a per-field validation `Select`, each with `FieldHelp`.
- **Conversational** — `profile/conversational-capture.ts`: `buildProfileCaptureInstructions` injected
  into the interviewer prompt (`question-stream.ts` `profileCapture` section) via `messages/route.ts`
  when conversational + non-anonymous + no snapshot; `extractAndPersistConversationalProfile` best-effort
  post-turn extraction → validate → upsert (only a COMPLETE profile is written, so the directive stops).

## Tests

- `tests/unit/lib/app/questionnaire/profile/validate-profile-fields.test.ts` — three modes, batching,
  non-fatal fallback, cost log.
- `tests/unit/lib/app/questionnaire/profile/conversational-capture.test.ts` — directive builder +
  extraction (complete persists, incomplete/empty don't, non-fatal).
- `tests/integration/api/v1/app/questionnaire-sessions/profile-route.test.ts` — GET/PUT, access modes,
  anonymous/not-applicable/active guards, authoritative server re-validation, no-login null userId.
- `tests/unit/components/app/questionnaire/profile/profile-capture-gate.test.tsx` — blocks-until-valid,
  `X-Session-Token`, `fieldErrors` mapping, `onSubmitted`.
- `tests/unit/components/app/questionnaire/session-workspace.test.tsx` (capture block) — gate shows on
  fresh form-mode, kickoff deferred until submit, toggle suppressed, skipped when satisfied / conversational /
  anonymous / read-only.
- Extended: `config-schema` (validation default + captureMode enum), `profile-values` (default injection),
  `start/page` (idempotent resume; needs-profile branch removed).

## Follow-up: hybrid placement (per-field `captureVia`)

Extends F8.7 so a single questionnaire can collect **some** fields in the form gate and **others**
conversationally — e.g. name + email up front, the rest in-chat. `captureMode` becomes the version-wide
**default** placement; each field may override it.

- **Decisions (confirmed):** per-field `captureVia` override (not a new top-level `hybrid` mode); and
  **persist partial, don't block** — required conversational fields keep the in-chat directive alive
  until answered, optional ones are taken as-offered.
- **Schema / types** — `ProfileFieldConfig.captureVia?: CaptureMode` (optional, NO default → "inherit");
  in the existing `profileFields` JSON, so **no column / no migration**. `config-schema` parses it;
  `parseProfileFields` (and thus `detail.ts`) passes it through.
- **Placement split (new, pure)** — `profile/capture-placement.ts`: `effectiveCaptureVia`,
  `splitFieldsByPlacement`, `conversationalCaptureActive` (required-missing → active; else active only
  until the first conversational value lands). Shared by the resolver and the interviewer so they can't
  disagree.
- **Resolver** — `ResolvedSessionCapture.fields` → **`formFields`** (the `form`-placement subset only);
  `satisfied = hasSnapshot || formFields.length === 0`. The old `captureMode === 'form'` gate condition
  is gone.
- **Snapshot** — `upsertProfileSnapshot` now **merges** into existing values (two-pass hybrid: form
  then chat) instead of overwriting. Single-pass modes are unaffected (merge over empty base).
- **Conversational** — `extractAndPersistConversationalProfile` validates + persists the **captured
  subset** partially (no all-required gate); `hasProfileSnapshot` → `readProfileSnapshotValues`; the
  messages route derives the conversational subset via the split and injects the directive per
  `conversationalCaptureActive`.
- **Surfaces** — workspace `showCapture`/gate read `formFields`; the anon-boot wire schema renames
  `fields` → `formFields` (+ optional `captureVia`); the admin editor relabels the mode control to
  **Default placement** and adds a per-field **Collect via** select (Default / Form / Conversational)
  with `FieldHelp`.
- **Tests** — new `capture-placement.test.ts`, `profile-snapshot.test.ts` (merge); extended
  `resolve-capture` (hybrid split + empty-form-subset satisfied), `conversational-capture` (partial
  persist + `readProfileSnapshotValues`), `profile-route` (`formFields`, not-applicable = empty subset),
  `config-schema` (`captureVia` parse/reject), `config-editor` (override preserved / omitted on save),
  `session-workspace` (no gate when `formFields` empty).
- **Edge (documented):** a hybrid version whose conversational subset is **all optional** and whose form
  gate runs first will ask for those optionals once (first opportunistic pass), then let them go — by
  design, to avoid every-turn nudging/extraction cost.

## No CHANGELOG entry

App-owned models/routes are not part of the Sunrise platform surface — per the repo's platform-scoped
CHANGELOG policy, F8.7 adds no `CHANGELOG.md` bullet.

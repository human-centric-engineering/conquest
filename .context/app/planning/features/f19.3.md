---
feature: F19.3
title: A questionnaire chooses where the text ladder opens
phase: P19 — Brand kit
status: done
owner: TBD
deps:
  The respondent text-size stepper (shipped without a tracker; see `chat-text-size.md`), F15.5 (the
  meeting participant boot, whose surface bundle has to carry the new field), F7.2 (the config
  Settings tab this lands on)
opened: 2026-08-28
closed: 2026-08-28
docs: .context/app/questionnaire/chat-text-size.md
---

# F19.3 — A questionnaire chooses where the text ladder opens

> The text-size stepper was entirely respondent-owned and had no authored counterpart. A demo run on
> a boardroom screen opened at the same size as one read alone on a laptop, and the only fix was
> asking the room to press the "A" twice.

## What was wrong

`chat-text-size.md` argued the size was deliberately **not** a config knob: an admin cannot know a
respondent's eyesight, screen or viewing distance. That argument is right about the size itself and
is untouched here.

It was doing double duty, though. It also ruled out the _starting_ size — which an admin often does
know, because they know the room. Somebody demoing on a projector, or shipping an instrument dense
enough to want smaller type, had no way to say so.

## What shipped

`config.chatTextSize` (`small` | `standard` | `large` | `largest`), on the Settings tab's
**Respondent experience** group, labelled **"Text size"**. It names the size the conversation opens
at.

**The ordering is the whole design.** The first cut got it wrong, shipped, and was corrected the
same day — see the amendment below. The rule now is **adopt once per authored value**:

```
useLocalStorage(CHAT_TEXT_SCALE_STORAGE_KEY, normalizeScaleIndex(chatTextScaleIndex))
  └── first paint, and the whole story for a respondent who has never stepped

adoption effect, after that hydration:
  authored === standard            → do nothing (the column default reads as "never set")
  authored !== cq-chat-text-authored.v1 → move to it, and record it
  authored === cq-chat-text-authored.v1 → leave the respondent's rung alone
```

So: an explicitly authored size is applied on arrival even to someone who has stepped before; step
away from it and your size stands; the author moves the setting and it is applied once more. An
author can say how the conversation opens and correct it later. They cannot pin or cap a size,
cannot repeatedly reset one, and there is still no switch that removes the stepper.

## Amendment — the setting shipped inert (2026-08-28)

The first cut passed the authored rung as `useLocalStorage`'s `initial` and stopped there, on the
argument that an author must never reach into an accessibility preference. `initial` applies only
while nothing is stored, so the setting did nothing at all for anyone who had ever touched the
stepper — **starting with the author previewing their own choice**, who is the person most likely to
have touched it. It was reported as "it isn't working", and it wasn't: it was invisible to everyone
who would ever check it.

The principle survives; the absolutism did not. A setting whose author cannot see it working is not
a setting, and "cannot be overridden, ever" is not the only way to protect a respondent's size —
"adopted once, yours thereafter" protects it just as well while leaving the feature real.

Worth remembering as a shape, not just a bug: the design reasoned entirely about **respondents** and
never about the **author**, so the one reader guaranteed to test it was the one reader the ordering
excluded.

## Confirmed decisions

- **A starting point, never a cap.** Stepping _down_ from an authored `largest` must work — a floor
  would pin every laptop respondent at 1.3 — and a test asserts it.
- **Standard is indistinguishable from "not set".** The column defaults to `standard`, so the
  adoption effect treats it as no instruction and leaves the respondent's own rung alone. That is
  what keeps a size carrying between questionnaires for the common case (an author who never touched
  this), and it is the one thing the rewrite kept from the original ordering. Tested.
- **Adopt ONCE per authored value, tracked in `cq-chat-text-authored.v1`.** Re-adopting on every
  mount would make the stepper useless on any questionnaire with an authored size — every reload
  would undo it. Tested from both sides: stepping away survives a remount, and moving the authored
  value re-adopts.
- **The adoption effect is declared after the `useLocalStorage` call, and that is load-bearing.**
  React queues effects in declaration order, so the authored rung lands after the stored one has
  hydrated. Above it, the stored rung wins and the setting is inert again — the original bug.
- **The column stores the NAME, not the ladder index.** localStorage can keep an index because its
  key is versioned (`.v1`) and a retune can discard stale ones. An authored value has no such
  escape: a stored `2` would quietly mean a different size once the multipliers moved, where a
  stored `large` still means the large one. `CHAT_TEXT_SIZES` is index-aligned with
  `CHAT_TEXT_SCALES` to make both true at once, and the ladder tests assert the alignment rather
  than assume it.
- **`indexForTextSize` must never return `-1`.** It is forgiving like every other plain-TEXT enum
  read in this codebase (a rollback, a seed, or a newer deploy's rung name can all reach the
  column), and it resolves anything unrecognised to `standard`. `-1` would arrive at the
  transcript's `calc()` as a `NaN` and drop its `font-size` declaration entirely — blanking the
  size for the whole conversation. A test covers `null`, `undefined`, `''`, an unknown rung, a
  prototype key, and — deliberately — the string `'2'`, so a caller who confused the name for the
  index gets a readable conversation rather than the large rung.
- **Index 1's label moved from "Default" to "Standard".** It is visible: the stepper's
  `role="status"` announcement reads it aloud. "Default" is actively wrong on a questionnaire whose
  author set the default to Large.
- **Resolvers return an INDEX, not a name.** The one consumer needs an index, so
  `resolveChatTextScaleIndexForVersion` and the surface bundle both do the lookup server-side and
  the four surfaces pass a number they cannot mis-map.
- **The meeting parity test was extended, not just the code.** `resolveRespondentSurfaceConfig` is
  checked field-for-field against the per-field resolvers. Without adding the new field there, a
  participant whose questionnaire opened at Largest would have silently dropped to standard the
  moment they entered a breakout room — the exact class of gap that test exists to catch.
- **Additive migration, default `'standard'`.** That is the size every questionnaire had before the
  column existed, so every live version renders unchanged. Generated with `--create-only`; the
  phantom pgvector `DROP INDEX` block was stripped before applying, and all five vector indexes
  were verified present afterwards.

## Files

| Path                                                          | What                                                      |
| ------------------------------------------------------------- | --------------------------------------------------------- |
| `lib/app/questionnaire/chat/text-scale.ts`                    | `CHAT_TEXT_SIZES`, `indexForTextSize`, `textSizeForIndex` |
| `prisma/schema/app-questionnaire.prisma` + migration          | `chatTextSize String @default("standard")`                |
| `lib/app/questionnaire/types.ts`                              | The config field and its default                          |
| `lib/app/questionnaire/authoring/config-schema.ts`            | The Zod write boundary                                    |
| `lib/app/questionnaire/settings-registry.ts`                  | The "Text size" row for the exported settings table       |
| `components/admin/questionnaires/config-editor.tsx`           | The Settings-tab control and its help copy                |
| `lib/app/questionnaire/chat/anonymity.ts`                     | `resolveChatTextScaleIndexForVersion`                     |
| `lib/app/questionnaire/session/resolve-respondent-surface.ts` | The meeting bundle's `chatTextScaleIndex`                 |
| `lib/hooks/use-session-workspace.ts`                          | The `initial` seam + the adoption effect — the ordering   |

## Still open

- **The stepper is chat-only.** The form surface has its own type sizing and neither the stepper nor
  this setting touches it. Unchanged by this work, but now slightly more surprising: an admin who
  sets "Text size: Largest" on a `form`-mode questionnaire gets nothing.
- **No preview.** The admin picks a rung without seeing it. The Settings tab has no live respondent
  preview to hang one off, so this would need the `?preview=1` surface to accept an override.
- **It reads as a flat row in the Questionnaire Pack.** The registry entry is `tier: 'standard'`, so
  `buildSettingRows` puts "Text size: Large" into the pack's setup listing with no note that a
  respondent can change it. A client reading the pack could take it for a fixed property of the
  instrument. The row wording used to hedge this ("Text opens at"); the label was renamed to "Text
  size" because the hedge read as jargon on the Settings tab, so the pack now needs its own note.

## Related

- [`f19.2.md`](./f19.2.md) — the typeface pairings, the other half of "how does this read?"
- [`../../questionnaire/chat-text-size.md`](../../questionnaire/chat-text-size.md) — "The
  respondent's setting; the questionnaire's starting point"
- [`../../questionnaire/configuration.md`](../../questionnaire/configuration.md) — the config field
  table

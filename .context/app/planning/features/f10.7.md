---
feature: F10.7
title: Data-slot weighting + confidence in the narrative report, and a config preview
phase: P10 — Reporting
status: done
owner: TBD
deps: F10.1 (respondent report modes 1+2), F10.2 (mode 3 narrative), F10.6 (questionnaire data in reports)
opened: 2026-07-14
docs: .context/app/questionnaire/respondent-report.md
---

# F10.7 — Data-slot weighting, confidence, and a config preview

> Committable tracker for **F10.7**. Two workstreams on the report-research-and-data branch:
>
> 1. **Data-slot influence + confidence** — the AI report can now weigh the conversational **data-slot**
>    understanding against the **direct answers** (a soft 0–100 knob, default 50/50), include data-slot
>    **rationales**, and surface **confidence** so the writer can discount low-confidence items.
> 2. **Config preview** — a **Preview report** button renders how the configured report will read, from
>    AI-synthesised sample answers, before going live. Reuses the real generation core.

## Intent

- The narrative report drew **only** on the direct question answers; the parallel data-slot layer (with
  its own rationale + confidence) never reached the writer, and confidence was never surfaced.
- Admins had no way to see how a report configuration would read before publishing (no real responses
  exist pre-launch).

## What shipped

**Data-slot influence + confidence (both AI modes):**

- Two `generation` config knobs — `dataSlotInfluence` (0–100, default `DEFAULT_DATA_SLOT_INFLUENCE = 50`)
  and `discountLowConfidence` (default `true`) — wired type → default → narrow → Zod (optional, for
  import back-compat) → editor (slider + switch, gated/hinted on `dataSlotsEnabled`).
- `loadSessionExport` now loads `rationale` + `confidence` onto each `ExportDataSlotGroup` slot
  (additive; the respondent-facing "Captured information" appendix ignores them).
- New pure `buildDataSlotContextBlock` (`report/content.ts`) flattens filled data slots into a themed
  context block (paraphrase + `Why:` rationale + optional `(confidence x.xx)`); `buildAnswerTranscript`
  gained an `includeConfidence` option that annotates each `A:` line.
- `buildReportMessages` folds the data-slot block into the system prompt, adds the weighting instruction
  (`100 - dataSlotInfluence`% answers / `dataSlotInfluence`% data-slot context — soft), and, when
  `discountLowConfidence`, a discount-low-confidence instruction. Skipped entirely when the version has
  no data slots.

**Config preview (AI modes only):**

- `generateRespondentReport` split into a loader + the exported `generateReportFromInputs(inputs)` core.
- `synthesiseSampleReportInputs` (`report/preview-sample.ts`) invents one sample respondent and maps
  it through the **same** transcript + data-slot builders. Originally one structured LLM call; now a
  persona pass plus a batched fan-out — see **Later fixes** below.
- New admin route `POST …/versions/:vid/report/preview` (`reportPreview` endpoint; `reportPreviewLimiter`,
  20/min/admin) — synthesise → force research + KB off → `generateReportFromInputs`. Rejects `raw` mode.
- Shared renderer extracted to `components/app/questionnaire/report/report-body.tsx`
  (`ReportBody` / `ReportPaperHeader` / `ReportDataAppendix` / …), re-imported by `session-complete.tsx`;
  the editor's **Preview report** button renders the result in a paper dialog behind a sample caveat.

## Decisions

- **Weighting is a soft prompt instruction**, not a deterministic filter — prose emphasis can't be
  enforced. It is inert without data slots (empty block ⇒ no instruction ⇒ pre-F10.7 behaviour).
- **Influence is independent of `rawIncludes`** — it feeds the _prompt_; `rawIncludes` controls the
  _displayed_ appendix. Feeding data slots as context is orthogonal to showing them.
- **Confidence is soft-disregard, not a hard floor** (per product decision) — surfaced + instructed,
  never a cutoff that silently drops answers.
- **Preview uses AI-synthesised answers** running the real generation core, so a previewed report and a
  live one share one path; web search + KB are forced off (fast/cheap/deterministic, no external deps).
  Only the AI modes preview (raw output = the answers, previewed via the respondent walkthrough).
- **No schema/migration** — both knobs live in the existing `respondentReport` JSON config column;
  data slots + fills load through existing relations.
- **Cohort-report preview deferred** — the extracted renderer, `generateReportFromInputs`, and the
  synthesiser are built to be reused for it later.

## Tests

- `content.test.ts` — `buildDataSlotContextBlock` (themed formatting, `includeConfidence` off, empty/
  undefined); `buildAnswerTranscript` confidence annotation.
- `settings.test.ts` — new knob defaults + `dataSlotInfluence` clamp/round.
- `config-schema.test.ts` — accepts configs with/without the new (optional) generation keys; rejects
  out-of-range influence.
- `session-export.test.ts` — `rationale` + `confidence` carried into `dataSlotGroups`.
- `generate.test.ts` — data-slot block + weighting + confidence instruction present/absent by config.
- `preview-sample.test.ts` — synthesis → shared builders; agent-not-seeded + parse-failure throws.
- `report-preview-route.test.ts` — gate order, non-AI rejection, not-found, success (no cost leak),
  research/KB forced off, 502, 429.
- `respondent-report-editor.test.tsx` — confidence toggle in the save payload; Preview button posts the
  current config to the preview endpoint.

## Later fixes

**2026-08-04 — preview failed on any real questionnaire (`fix/error-sweep`).** Reported as
`Report preview failed … "Request timed out."`. The sample answerer asked **one** LLM call to emit an
entry per question **and** per data slot, capped at 3 000 output tokens / 45 s. Measured against a real
71-question / 30-slot version on `gpt-5.4`: the full sample needs **~7 060 output tokens and ~84 s**, so
the single call could only ever fail — it hit the token cap at exactly 3 000, truncated mid-JSON, failed
the parse, burnt the retry, and threw; on a slightly larger version or a slower moment the 45 s timeout
won the race instead, which is the message that reached the admin. Both were the same undersized budget.

Fixed the way `data-slots/generate-stream.ts` already solves the identical "one call truncates on large
questionnaires" problem — fan out:

- **Persona pass** — one small call (700 tokens) invents the respondent up front, because independent
  batches would otherwise each invent a different person and the preview would read as several people.
  Its failure is tolerated (batches fall back to a self-describing instruction).
- **Batch passes** — items split into groups of ~20, `SAMPLE_BATCH_MAX_TOKENS` (2 800) / 90 s each, run at
  concurrency 6 via the extracted `llm/run-with-concurrency.ts` (also now used by the data-slot
  generator, whose private copy invoked the callback once on an empty list). A failed batch costs its
  items, not the preview — the coverage block reports the gap honestly rather than throwing.
- **Bracketed-key normalisation** — the separate, previously-silent bug this surfaced. The prompt lists
  items as `- [q1] Mood?` and the model echoes `"key": "[q1]"`; the brackets matched no question, so
  entries were dropped at the mapping step and rendered as _unanswered_. In the first fixed run **54 of
  71 answers** were being discarded this way (coverage read 17/71). `normaliseKey` strips one layer of
  wrapping brackets, and a non-matching key now logs a warning instead of vanishing.
- **Route** — `maxDuration = 300` (also in `vercel.json`; the flow measures ~105 s end to end and the
  platform default is 60 s), the failing stage recorded in the error log, and a timeout answered as
  **504 `REPORT_PREVIEW_TIMEOUT`** rather than a blanket 502, since only a timeout is worth retrying.

Verified end to end against the 71-question version: **71/71 answers and 30/30 data slots**, ~36 s
synthesis + ~68 s generation. The live report path is untouched — the sample answerer is preview-only.

Added tests: `preview-sample.test.ts` (fan-out covers every question, disjoint batch slices, partial
degradation, bracketed keys, persona shared across batches + tolerated failure, cost summation),
`run-with-concurrency.test.ts` (limit respected, pipe kept full, empty list, rejection propagation),
`report-preview-route.test.ts` (504-vs-502 across all three timeout error shapes, per stage).

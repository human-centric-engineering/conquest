---
name: Conversational Questionnaire
status: building
host_platform: sunrise
sunrise_version: 0.0.1
opened: 2026-05-30
supersedes: Conversational Questionnaire Phases.md
---

# Conversational Questionnaire — project plan

> The working plan for the Conversational Questionnaire app. Outlines the concept, key requirements, and the phased build broken into **features** and (when promoted) **tasks**. Mirrors the working model of the future [[v1-requirements|HCE Hub]] — until the Hub exists, this markdown is the system of record. Replaces the earlier prompt-per-phase document, which is preserved as reference detail.

## How to read this

- **Project → Phase → Feature → Task.** Aligned to the [[v1-requirements|HCE Hub]] data model. A **Phase** is a milestone container — release boundary, future-work parking — scaffolded into the Hub v1 schema and consumed by Hub UI/capabilities from v1.x (see [[v1-requirements#10. Initial data model sketch]] and [[futures#Coarse work grouping — Phases / Epics]]). This plan's informal `P0..P9` naming is the precursor; when v1.x phase UI lands, those phases become real `Phase` rows. A **Feature** is a coherent multi-PR capability with one owner, status arc, and dependencies. A **Task** is a PR-sized work unit, declared (promoted) under a feature when its owner is ready.
- **Intent over prescription.** Each phase and feature captures _what_ and _why_, not _how_. Implementation choices are made at the moment of work, with current context, by the owner + Claude.
- **Stable identifiers.** Phases are `P0..P9` (plus `P2.5`); features are `F<phase>.<n>` (e.g. `F4.1`); tasks are `T<feature>.<n>` (e.g. `T4.1.2`). Reference them when asking Claude to plan a piece of work: _"let's plan F4.1."_
- **Decisions and work-to-date are first-class.** See the running [[#Decisions log]] and [[#Work completed to date]] sections at the end. Append, don't rewrite.
- **The plan is allowed to be wrong.** Successful builds always deviate from the plan as insights, problems, and solutions arise. Edit it as you go; record material reframes in the decisions log.

## Project

| Field         | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Name          | Conversational Questionnaire                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Repo          | `human-centric-engineering/conquest` (forked from `human-centric-engineering/sunrise` at v0.0.1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Host platform | Sunrise v0.0.1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Lead          | Simon Holmes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Status        | `building` — P0 done (F0.1, PR #10); P1 done (F1.1 ingestion: PR #13/#14/#15); P2.5 done (F2.5.1, #21); P2 done (F2.1 #18/#19, F2.2 #23, F2.3 #24, F2.4 #25); P3 done (F3.1 #26, F3.2 #27, F3.3 #28, F3.4 #29); P4 done (F4.1 #30, F4.2 #32, F4.3 #33, F4.4 #34, F4.5 #35, F4.6 #36); P5 done (F5.1 #37, F5.2 #38, F5.3 #39); P6 done (F6.1 #40, F6.2 #41, F6.3 #42, F6.4 #43); P7 done (F7.1 #44, F7.2 #45, F7.3 #46, F7.4 #47) + admin upload-questionnaire UI (#48). **Next: P8** (analytics, exports, anonymous-mode hardening), then P9. A 2026-06-07 audit found five deferred items whose home feature shipped but which were never delivered — being closed on `feat/close-deferred-gaps` (see decisions log). |
| Opened        | 2026-05-30                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

---

## Concept and intent

A conversational questionnaire platform built on Sunrise. An admin uploads a questionnaire document (PDF / DOCX / MD); an agent extracts questions and sections from it; end users complete the questionnaire through a streaming conversation rather than form-filling; the LLM extracts, infers, and synthesises answers with confidence scores and provenance; admins review the structure, evaluate it against goal and audience, manage versions, and export results.

The platform is **provider-agnostic** — it resolves models through Sunrise's `AiProviderModel` registry at runtime and runs against whatever provider the prospect uses (Anthropic, OpenAI, Google, OpenRouter, etc.). Nothing locks to a single vendor.

### Two audiences, one codebase

The platform serves two purposes simultaneously, and every decision should preserve both:

1. **A sales-demo vehicle.** It demonstrates Agentic Sunrise's capability to prospects — consultants, survey-using businesses, anyone curious what agentic LLM applications do beyond chat-bot demos. A prospect should see their own brand, their own questionnaire content, and their own users completing it within an hour of the discovery call.
2. **A project starter.** When a prospect signs an engagement to build a real product in this style, the platform forks into the starting point for that project. Fork-readiness is therefore non-negotiable: clean architectural seams, marked demo-only code, public-surface discipline, and a fork-procedure guide for the inheriting team.

Where a single decision serves only one purpose, mark it explicitly with `// DEMO-ONLY:` so a fork knows what to strip.

### Reuse beyond questionnaires

Some of what this platform builds is questionnaire-specific. Some is reusable across any agentic-application domain. Forks should understand the boundary.

**Questionnaire-specific** (rename or rebuild for a non-questionnaire fork):

- The Prisma schema's `AppQuestionnaire`, `AppQuestionSlot`, `AppAnswerSlot`, `AppQuestionnaireSession`, related models
- Extraction capabilities (parses questionnaire-shaped documents)
- Answer-into-slot extraction
- Selection strategies operating on questions
- The user-facing split-screen UI with the answer-slot panel

**Genuinely reusable** (carry forward to non-questionnaire forks):

- The **per-turn orchestrator** pattern (P6)
- The **design-time evaluation** pattern (P5) — admin reviews a structured artefact against a stated goal/audience via agents-as-judges
- The **demo tenancy + theming** module — a cross-cutting concern in marked `// DEMO-ONLY:` modules; foundation in P2.5, the rest distributed across P3/P6/P7/P9
- The **change-record review-and-revert** pattern (P1)
- The **suggestion review-and-accept** pattern (P5)
- The **tag-and-analytics-filter** pattern (P2, P8)
- Audit log, cost-tracking, feature-flag, versioning — Sunrise primitives consumed throughout

---

## Key requirements

### Functional

- **Ingest** a questionnaire document and produce a structured, editable representation of its sections and questions, with confidence and rationale per extraction decision (reviewable and revertible).
- **Author and configure** a questionnaire: edit structure, add/remove questions, tag, version, configure selection strategy, completion thresholds, anonymous mode, voice toggle, contradiction-detection cadence, per-session cost cap, profile-fields-to-collect.
- **Evaluate** a questionnaire's structure against its stated goal and audience using Sunrise's agents-as-judges; review and selectively apply suggestions.
- **Invite** users with a tokenised flow; track invitation state through to completion.
- **Converse** with a user through a streaming chat that selects, asks, and synthesises answers from natural language — with provenance, confidence, contradiction detection, and a clear completion offer.
- **Capture** answers as `AppAnswerSlot` rows with `ProvenanceItem` records (consumed from Sunrise's contract, not redefined).
- **Analyse + export** results: per-question distributions, completion funnel, cost actuals, CSV/JSON export, anonymous mode.
- **Brand** the user experience per demo-client tenant for the sales context.

### Non-functional

- **Provider-agnostic.** Every LLM call resolves through Sunrise's provider manager.
- **Cost-aware.** Per-agent budgets and per-session caps enforced via Sunrise's existing budget mechanism; admin sees actuals via `AiCostLog`.
- **Auditable.** Every admin mutation produces an `AiAdminAuditLog` row via `logAdminAction()`. Session-state transitions produce an `AppQuestionnaireSessionEvent`.
- **Provenance.** Every extracted answer carries non-empty `ProvenanceItem` records.
- **Versioned.** Edits to a launched version (any sent invitation or in-progress session) fork a new draft; in-flight sessions stay pinned to the version they started on.
- **Segregated.** All questionnaire logic lives under `lib/app/questionnaire/` (platform-agnostic, no Next imports); HTTP under `app/api/v1/app/`; admin pages under `app/admin/questionnaires/`; user pages under `app/(protected)/questionnaires/`. Sunrise primitives consumed through public entry points.
- **Feature-flagged.** `APP_QUESTIONNAIRES_ENABLED` (DB-backed) gates every surface.

---

## Architecture summary

### Sunrise primitives consumed (v0.0.1)

| Need                                       | Sunrise primitive                                                                                                    |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| Agents (extractor, judges, conversational) | `prisma.aiAgent.create()` + `AiAgentVersion` versioning                                                              |
| Workflows (intake, evaluation, completion) | `prisma.aiWorkflow.create()`                                                                                         |
| Capabilities                               | `capabilityDispatcher.register()` + `AiCapability` seeded row; registered via Sunrise's app-capability hook (seam 3) |
| App models                                 | Own `prisma/schema/app-questionnaire.prisma` (seam 1)                                                                |
| App seeds                                  | `prisma/seeds/app-questionnaire/NNN-*.ts` discovered by `db:seed` (seam 2)                                           |
| Admin nav                                  | Sunrise nav registry (seam 4) — no edit to the sidebar component                                                     |
| `lib/app/**` import boundary               | Shipped by Sunrise (seam 5)                                                                                          |
| User-relation pattern                      | Plain `String` FK to `User.id`, no `@relation`; documented FK/cascade recipe (seam 6)                                |
| App env vars                               | Sunrise app-env extension surface (seam 11)                                                                          |
| Document parsing                           | `parseDocument()` + `previewDocument()` / `confirmPreview()`                                                         |
| Embeddings                                 | `embedText()` / `embedBatch()`                                                                                       |
| Streaming chat                             | `streamChat()` + `sseResponse()` + `withAuth` / `withAdminAuth`                                                      |
| Voice / attachments                        | `getAudioProvider()`, `useVoiceRecording`, `<MicButton>`, `chatAttachmentSchema`, `assertModelSupportsAttachments()` |
| Evaluation                                 | `AiEvaluationRun`, `AiEvaluationCaseResult`, grader registry, admin eval UI                                          |
| Audit                                      | `logAdminAction()`                                                                                                   |
| Cost                                       | Per-agent budgets + `AiCostLog` query helpers                                                                        |
| Feature flag                               | `isFeatureEnabled('APP_QUESTIONNAIRES_ENABLED')`                                                                     |
| PDF rendering                              | App-owned dependency (vertical, not promoted upstream)                                                               |

### Relationship to Sunrise

The canonical model is [[building-on-sunrise]]: **public-surface-first, fix-in-place, promote-upstream**. The pre-fork seams the questionnaire planning surfaced are closed in Sunrise v0.0.1 (see [[fork-readiness-backlog]]). The app builds on top of those seams rather than working around them.

When a need arises that Sunrise's public surface doesn't cover, the rule is _not_ "flag and work around" — it is fix-in-place where possible, then **classify**: a generic primitive or seam goes upstream to Sunrise _promptly_; an app-specific behaviour stays in the app, plugged into an extension point. Carried changes (not yet upstream) are tracked in this doc's [[#Carried Sunrise patches]] section, retired when Sunrise releases include them.

---

## Phases overview

The build moves from scaffolding → ingestion → admin manage → demo branding → configuration → conversational core → evaluation → streaming → user UI → analytics → hardening. Phases are sequenced so each one's surface area is exercisable end-to-end before the next adds new abstraction.

| Phase    | Title                                           | Status      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------- | ----------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P0**   | Foundations                                     | done        | F0.1 shipped (PR #10) — substantially lighter than the original plan; Sunrise v0.0.1 provides the seams that used to need workarounds.                                                                                                                                                                                                                                                                                                                                |
| **P1**   | Questionnaire ingestion                         | done        | Admin uploads a doc; LLM extracts structure; changes recorded for review. API-only. F1.1 (P1's sole feature) complete: PR1 (schema) + PR2 (pure core) in PR #13; PR3 (extractor capability) in PR #14; PR4 (ingestion route + persistence) in PR #15.                                                                                                                                                                                                                 |
| **P2**   | Admin CRUD over questionnaires                  | done        | Admin UI: list, edit, version, tag, review extraction changes. All four features merged: F2.1 authoring (PR #18/#19); F2.2 tagging (#23); F2.3 extraction-change review (#24); F2.4 re-ingest (#25).                                                                                                                                                                                                                                                                  |
| **P2.5** | Demo-client foundation                          | done        | F2.5.1 shipped (PR #21) — demo-client identity + `AppQuestionnaire` FK + admin attribution, the slice that must **lead** so P3+ build tenant-aware. The rest of demo tenancy (theming, invitation branding, session reset, content seed) is **distributed** into P3/P6/P7/P9 as marked `// DEMO-ONLY:` sub-features.                                                                                                                                                  |
| **P3**   | Configuration, invitations, and cost estimation | done        | Per-version config; invitation flow; pre-launch cost estimate. All four features merged: F3.1 config (#26); F3.2 invitation flow (#27); F3.3 cost estimation (#28); F3.4 demo-client invitation branding (#29).                                                                                                                                                                                                                                                       |
| **P4**   | Conversational engine (non-streaming)           | in flight   | Selection · extraction · contradiction · completion logic, exercised without the streaming surface. F4.1 selection strategies (#30) + F4.2 answer extraction (#32) + F4.3 contradiction detection (#33) + F4.4 answer refinement (#34) + F4.5 completion logic (#35) merged; F4.6 session state machine in flight.                                                                                                                                                    |
| **P5**   | Design-time evaluation (agents-as-judges)       | done        | Judges score a questionnaire against goal/audience; suggestion review queue. F5.1 judge agents done (app-native dispatch — `evaluate-structure` capability + 7 `kind='judge'` seeds + preview route); F5.2 run persistence done (synchronous run route + app-owned run/finding models + admin run history); F5.3 review queue done (structured `proposedEdit` ops on the findings contract + apply-through-fork engine + read-time staleness + interactive queue UI). |
| **P6**   | Conversational session (streaming)              | done        | F6.1 per-turn orchestrator + streaming (#40); F6.2 voice input (#41); F6.3 cost-cap enforcement (#42); F6.4 demo session reset (#43). _Attachment input deferred → see audit gap-fill (Item 5)._                                                                                                                                                                                                                                                                      |
| **P7**   | User-facing conversational UI                   | done        | F7.1 chat surface (#44); F7.2 answer-slot panel (#45); F7.3 session lifecycle UX (#46); F7.4 PDF export (#47); admin upload-questionnaire UI (#48). _Attachment input + invite claim-via-login deferred → see audit gap-fills._                                                                                                                                                                                                                                       |
| **P8**   | Admin analytics, exports, anonymous mode        | not started | Dashboards, CSV/JSON export, anonymous-mode handling.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **P9**   | Hardening + forking docs                        | not started | Runbook, flag inventory, `forking.md`, concurrent-session sanity.                                                                                                                                                                                                                                                                                                                                                                                                     |

---

## P0 — Foundations

**Intent.** Stand up the app's territory inside the Sunrise fork: module structure, schema, feature flag, doc namespace, capability registration, test scaffolding. Produces no user-visible feature; everything that follows depends on this being right. Significantly lighter than the original Phase 0 because Sunrise v0.0.1 closed the pre-fork seams (multi-file schema, recursive seeds, capability hook, nav registry, eslint boundary, FK pattern, app env vars).

### F0.1 — Foundation scaffolding

_Status:_ shipped ([PR #10](https://github.com/human-centric-engineering/conquest/pull/10)) · _Size:_ ~1 PR · _Owner:_ Simon Holmes · _Deps:_ none (first feature)

The platform's home in the fork. Module skeleton, app-owned Prisma schema, seed namespace, capability hook wired, env-var surface, feature flag, doc namespace, and the test/healthcheck scaffolding the rest of the build hangs off.

_Indicative tasks:_

- App module skeleton at `lib/app/questionnaire/**` — sub-module dirs with stub `index.ts` / `types.ts`. Inherits Sunrise's `lib/app/**` ESLint boundary.
- App Prisma schema file at `prisma/schema/app-questionnaire.prisma` — every model prefixed `App…`, User FKs as plain `String` per seam 6 recipe.
- Initial migration via `prisma migrate dev --name app-questionnaire-init`. Verify it applies cleanly against a Sunrise v0.0.1 DB.
- App seed namespace at `prisma/seeds/app-questionnaire/` — discovered by recursive `db:seed`. First seed populates the `APP_QUESTIONNAIRES_ENABLED` flag row.
- App capability-registration hook wired with an empty set (populated from P1).
- App env-var declaration surface plumbed through Sunrise's app-env extension (empty at P0).
- Feature-flag wrapper `isQuestionnairesEnabled()` over Sunrise's `isFeatureEnabled()`.
- Healthcheck route at `app/api/v1/app/_healthcheck/route.ts` — 404 when flag off, 200 when on. Sets the gating template every later route follows.
- Doc namespace under `.context/app/` — `questionnaire/` for domain/technical docs (`README.md`, `overview.md`, `schema.md`, `development.md`); `planning/` for the build plan, `features/` trackers, and an empty forward-looking `upstream-gaps.md` ledger; plus an app-docs `README.md` index at the `.context/app/` root.
- Test scaffolding — unit + integration test trees mirroring source; a schema-shape integration test asserting every model/column/index/FK from `information_schema`.

**Definition of phase complete.** Schema migrates cleanly; `npm run type-check` / `npm run lint` / `npm run test` pass; healthcheck flips with the feature flag; seeds run via `db:seed`; doc namespace populated.

---

## P1 — Questionnaire ingestion

**Intent.** Let an admin upload a questionnaire document (PDF / DOCX / MD / TXT) and have an LLM extract sections, questions, types, and supporting metadata into the app's schema. Every extraction decision is recorded as an `AppQuestionnaireExtractionChange` so the admin can review and revert in P2. API-only — no UI yet.

### F1.1 — Document → questionnaire ingestion

_Status:_ done — PR1 (schema) + PR2 (pure extraction core) in [PR #13](https://github.com/human-centric-engineering/conquest/pull/13); PR3 (extractor capability) in [PR #14](https://github.com/human-centric-engineering/conquest/pull/14); PR4 (HTTP + persistence) in [PR #15](https://github.com/human-centric-engineering/conquest/pull/15) — F1.1 feature-complete · _Size:_ multi-PR · _Owner:_ TBD · _Deps:_ F0.1

> Committable tracker: [`planning/features/f1.1.md`](features/f1.1.md) — the indicative-task list below predates the reconciliation in that doc (e.g. it cites `previewDocument`/`confirmPreview`, which F1.1 deliberately does **not** use — it parses directly via `parseDocument`). Defer to the feature tracker for current scope.

End-to-end pipeline from uploaded document to populated `AppQuestionnaire(Version|Section|Slot)` graph with confidence per slot, plus the change-record audit trail of every extraction decision. Audit + cost integration are cross-cutting tasks within this feature, not a separate one.

_Indicative tasks:_

- Seed the extraction agent (`app-questionnaire-extractor`) + its capability set via `prisma.aiAgent.create()`. Follow the `006-quiz-master.ts` shape.
- Wire the upload + parse pipeline against Sunrise's `previewDocument()` / `confirmPreview()` (SHA-256 dedup, scanned-page detection, opt-in table extraction). Do not parallel them.
- Structure extraction prompt + capability — produces sections, questions, types, confidence.
- Goal + audience inference, optional, recorded as `infer_goal` / `infer_audience` change records.
- Change-record write path covering every change type (`prune_*`, `correct_*`, `rewrite_prompt`, `infer_type`, `merge_questions`, `split_question`, `add_section`, `augment_question`, `infer_goal`, `infer_audience`) with before/after JSON.
- Ingestion API endpoint at `POST /api/v1/app/questionnaires` — multipart upload, `withAdminAuth`, Zod-validated, returns the new version id.
- Audit (`logAdminAction()`) on admin mutations + cost integration via Sunrise's existing cost tracker.

**Definition of phase complete.** Uploading a representative PDF produces a populated `AppQuestionnaire(Version|Section|Slot)` graph plus a complete change-record log. Integration tests cover happy path and the three big failure modes (scanned PDF, oversized doc, unparseable type). Audit + cost logs populated.

---

## P2 — Admin CRUD over questionnaires

**Intent.** The first admin UI. Lets an admin list questionnaires, edit structure, manage tags, review and revert extraction changes from P1, and trigger re-ingestion. Implements the version-fork-on-launch behaviour. Four ownerable features.

### F2.1 — Questionnaire authoring

_Status:_ done — PR1 (read surface + admin shell) merged ([PR #18](https://github.com/human-centric-engineering/conquest/pull/18)); PR2 (structural authoring + version-fork) merged ([PR #19](https://github.com/human-centric-engineering/conquest/pull/19)) — full section/question CRUD + reorder, goal/audience edit, live version-fork-on-launched, status transitions · _Size:_ multi-PR (2 PRs) · _Owner:_ TBD · _Deps:_ F1.1

> Committable tracker: [`planning/features/f2.1.md`](features/f2.1.md). Confirmed at planning: **full structural authoring** in PR2; **build the full version-fork mechanics now**, behind a `countLaunchBlockers()` seam that returns 0 until P3 (invitations) / P4 (sessions).

The main admin surface: nav entry, list view, detail/edit view, and the version-fork-on-launched lifecycle. The first place an admin "lives" in the platform. Packaged as two PRs per the gate-suite-tax calibration — the read surface (no high-stakes mutation) takes one clean review pass; the write surface (mutation + fork correctness) justifies its own `/security-review`.

_Indicative tasks:_

- Register admin nav entry via Sunrise's nav registry (seam 4). No edit to `admin-sidebar.tsx`.
- Questionnaire list view at `app/admin/questionnaires/` — status, owner, version, last activity.
- Detail/edit view — edit sections, questions, types, goal, audience.
- Version-fork-on-launched behaviour — edits to a version with zero sessions and zero sent invitations mutate in place; edits to a launched version fork a new `draft` version. In-flight sessions stay pinned to the version they started on.
- Audit-log all admin mutations through `logAdminAction()` with before/after.

### F2.2 — Tagging

_Status:_ done — merged ([PR #23](https://github.com/human-centric-engineering/conquest/pull/23)) — schema + pure core + fork-copy + tag CRUD/assignment APIs + editor/read UI + tests · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F2.1

> Committable tracker: [`planning/features/f2.2.md`](features/f2.2.md). Build decisions: replace-set `PUT` assignment; tag = label + optional `color` (allowlist); same-version invariant is application-layer (checked pre-fork); the F2.1 `// F2.2:` fork seam now copies the vocabulary + re-links assignments.

Per-version tag vocabulary plus M:N assignment to questions. Used by P8 analytics filtering and by the adaptive selection strategy in F4.1.

_Indicative tasks:_

- `AppQuestionTag` CRUD (vocabulary editor in the version's edit view).
- `AppQuestionSlotTag` M:N assignment UI.
- Validation that tag/question both belong to the same version (application-layer per the schema decision).

### F2.3 — Extraction-change review

_Status:_ done — merged ([PR #24](https://github.com/human-centric-engineering/conquest/pull/24)) — pure revert planner + list/revert APIs + sub-route review UI + tests · _Size:_ 2 PRs (read surface · revert engine) · _Owner:_ TBD · _Deps:_ F1.1, F2.1

> Committable tracker: [`planning/features/f2.3.md`](features/f2.3.md). Build decisions: **full revert across all change types** (not just the tractable ones); review surface on a **dedicated sub-route** (`…/[id]/extraction-changes?v=`), not a detail-page tab; **fail-cleanly** reconciliation (null-`targetEntityId` edits matched against the live graph, typed `RevertImpossible` → 422 rather than guess); fork-on-launched applies the inverse to the draft and marks the **source** change row reverted.

Lists every `AppQuestionnaireExtractionChange` with source quote, before/after, rationale. Admin can revert any change; revert restores the pre-change graph state. This is the consumer of P1's change-record log.

_Indicative tasks:_

- Review surface listing changes by version, grouped by change type.
- Revert action that restores `beforeJson` and updates change-record status to `reverted`.
- Filters (by status, type, target entity).

### F2.4 — Re-ingest

_Status:_ done — merged ([PR #25](https://github.com/human-centric-engineering/conquest/pull/25)) — shared ingest pipeline refactor + `reingestVersion` replace-in-place writer + draft-only route with version-scoped dedup short-circuit + detail-page re-ingest dialog + tests · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F1.1, F2.1

> Committable tracker: [`planning/features/f2.4.md`](features/f2.4.md). Build decisions (confirmed): **replace the draft graph in place** (not a new version); **draft-only** — a launched/archived target is a typed `409 REINGEST_NOT_DRAFT`, not a fork; **version-scoped dedup** short-circuits an identical re-upload to a `200` no-op (vs. F1.1's global `409`); the F1.1 ingest "bytes → extraction" pipeline is factored into `_lib/extract-pipeline.ts` + `writeGraph`/`writeSourceDocument`, single-sourced by both routes (F1.1's tests are the regression net). No migration (every column, incl. the reserved `bytes`, exists from F1.1).

Admin uploads a replacement source doc against an existing draft version; SHA-256 dedup short-circuits an identical re-upload; non-identical re-upload produces a fresh extraction + change log.

_Indicative tasks:_

- `POST /api/v1/app/questionnaires/:id/versions/:vid/reingest` endpoint.
- SHA-256 dedup short-circuit returning the existing change log unchanged.
- UI surface to trigger re-ingest from the detail view.

**Definition of phase complete.** An admin can ingest, review, edit, tag, version, and re-ingest a questionnaire end-to-end through the UI.

---

## P2.5 — Demo-client foundation

**Intent.** Stand up the demo-client _identity_ — the table, the `AppQuestionnaire` foreign key, and the admin attribution surface — so a questionnaire can be labelled as a given prospect's demo. This is the one slice of demo tenancy that must **lead**: with the FK in place from here on, P3 (invitations), P4/P6 (sessions), and P7 (user UI) are all built tenant-aware in a single pass instead of retrofitted.

**This phase used to be all of demo tenancy; it isn't anymore.** Demo tenancy + branding is a _cross-cutting concern_, not a milestone — the decimal in "P2.5" was the tell that it was inserted between P2 and P3 without the dependency pass the round-numbered phases got. Most of it can only be built once the model each piece hangs off exists: invitation branding needs P3's invitation model, session reset needs P4/P6's session graph, the theming render points need P7's user pages, the content seed needs a complete vertical. (The original F2.5.1 even listed `AppQuestionnaireSession` scoping — a model P4/P6 hasn't built yet.) So those pieces now land **in the phase that builds their dependency**, each as a clearly-marked `// DEMO-ONLY:` sub-feature — see the distributed-work table below.

**Decompose the schedule, preserve the module boundary.** Fork-strippability — the property that justified bundling this as a phase — is a property of _code organisation_, not _build scheduling_. Every distributed piece still lives in the same marked territory (`lib/app/questionnaire/theming/`, the `demo-clients/` route + page dirs, `// DEMO-ONLY:` headers), so a fork still strips demo tenancy in one `grep` sweep even though the work is spread across phases. See the [[#Decisions log]] entry (2026-06-03).

### F2.5.1 — Demo-client foundation

_Status:_ done — shipped in [PR #21](https://github.com/human-centric-engineering/conquest/pull/21) · _Size:_ 1 PR · _Owner:_ Simon Holmes · _Deps:_ F0.1

> Committable tracker: [`planning/features/f2.5.1.md`](features/f2.5.1.md). Confirmed at build: identity-only (theme/logo deferred to F3.4/F7.1); FK `onDelete: SetNull` + app-layer 409 delete-guard; attribution via `PATCH /questionnaires/:id`. Packaged as **one PR** — no live destructive endpoint, no upload, no LLM, so one dominant risk type.

A **demo-client partition** — identity table, the `AppQuestionnaire` FK, and admin attribution — so each prospect's questionnaires are grouped and labelled under their name. Deliberately lightweight and app-owned: application-layer scoping on single-tenant Sunrise, **not** a security isolation boundary. Demo clients aren't adversarial, so hard isolation is out of scope here. Theme fields, session/invitation scoping, and the reset/seed utilities are **not** here — they ride into later phases (table below).

> **Not the bones of real multi-tenancy.** This table is a branding/content partition, not the foundation a real customer-isolation layer plugs into. If a fork becomes a multi-customer product, the right move is to activate Sunrise's RLS tenancy seam (`TENANCY_MODE=multi`, `Org`/`orgId` retrofit at the `lib/db/client.ts` chokepoint), **not** to harden this demo table into an isolation mechanism. Promoting app-layer demo scoping into a security boundary is exactly the trap Sunrise's multi-tenancy doc warns against. See the [[#Decisions log]] entry.

_Indicative tasks:_

- App-owned `AppDemoClient` **identity** table (`slug`, `name`, `description`, `isActive`), marked `// DEMO-ONLY:` — the abstraction a fork repurposes for branding/content scoping, not for isolation. Theme columns are deferred to the theming module (lands with its first consumer — see table).
- Nullable `demoClientId` FK on `AppQuestionnaire` (plain `String` FK per seam 6, indexed). `null` = generic Sunrise-default demo; pre-P2.5 questionnaires keep working, no backfill.
- Admin CRUD under `app/api/v1/app/demo-clients/` (list / create / detail / patch / delete; `DELETE` refuses `409` while any questionnaire still references the client).
- Attribution in the F2.1 admin surface — assign a questionnaire to a client and show the client in the list/detail. `PATCH …/questionnaires/:id` gains an optional `demoClientId` (nullable to detach).

**Definition of feature complete.** An admin can create demo clients and attribute questionnaires to them; the F2.1 list/detail surface shows attribution; the FK is in place for downstream phases to build tenant-aware.

### Distributed demo-tenancy work (tracked in host phases)

The rest of what was P2.5 lands where its dependency is built. Every piece stays in marked `// DEMO-ONLY:` modules so the fork-strip stays a single sweep.

| Piece                                                                       | Why it can't lead                                                  | Home                                              |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------- |
| Theming module (theme columns + `resolveTheme()` + `themeToCssVariables()`) | Pure producer; its first renderer is an invitation email or a page | **F3.4** if themed invites adopted, else **F7.1** |
| Invitation `demoClientId` denormalisation + themed invitation email         | Needs `AppQuestionnaireInvitation`                                 | **F3.4** (P3)                                     |
| Tenant-scoped user routing (resolve tenant from URL / invite token)         | Needs user-facing routes + invite tokens                           | **F7.1** (P7)                                     |
| Theming application points (landing / completion pages)                     | Needs P7 user pages                                                | **F7.1 / F7.3** (P7)                              |
| Demo session reset (`reset-sessions`)                                       | Needs the full session graph                                       | **F6.4** (P6)                                     |
| Clone-for-client admin utility                                              | Needs tags (F2.2) + config (F3.1)                                  | **done 2026-06-07** (deferred-gaps audit, Item 4) |
| Demo content seed (`010-demo-content.ts`)                                   | Needs a complete themeable vertical                                | **F9.4** (P9)                                     |

---

## P3 — Configuration, invitations, and cost estimation

**Intent.** Make a questionnaire launchable. Per-version configuration; invitation flow with tokenised registration; pre-launch cost estimation so the admin sees expected spend before sending invites.

### F3.1 — Questionnaire configuration

_Status:_ done — merged ([PR #26](https://github.com/human-centric-engineering/conquest/pull/26)) — `AppQuestionnaireConfig` editor (selection strategy, thresholds, caps, voice/anonymous/contradiction toggles, profile fields) + launch gate · _Size:_ ~1–2 PRs · _Owner:_ TBD · _Deps:_ F2.1

The full `AppQuestionnaireConfig` editor — every setting that controls how a session runs — plus the launch gate that says "config is complete enough to invite users."

_Indicative tasks:_

- Config editor UI — selection strategy, completion thresholds, cost budget, per-session cap, voice toggle, contradiction-detection mode + N, anonymous mode.
- User-profile-fields configuration — admin defines which profile fields are collected at session start. Field-type enum `text | email | number | select`. Supports name, email, role, organisation, team, tenure, arbitrary custom fields.
- Launch gate — questionnaire can only be launched when goal + audience + at least one section + at least one question + config are all populated.
- Validation, audit-log, version-fork-on-launched compatibility.

### F3.2 — Invitation flow

_Status:_ done — merged ([PR #27](https://github.com/human-centric-engineering/conquest/pull/27)) — PR1: `AppQuestionnaireInvitation` + pure core + admin send/list/revoke/resend routes + launch-blocker wiring + admin sub-route UI; PR2: public token metadata/accept routes + respondent registration page reaching `registered`. `started`/`completed` left as P6/P7 seam states; demo-client branding is F3.4 · _Size:_ 2 PRs · _Owner:_ TBD · _Deps:_ F3.1

> Committable tracker: [`planning/features/f3.2.md`](features/f3.2.md). Build decisions (confirmed at planning): dedicated **version-scoped** `AppQuestionnaireInvitation` (not Sunrise's `Verification` store); **full better-auth registration** (reuses the `accept-invite` machinery, binds `userId`); **single + simple bulk** send; first non-zero `countLaunchBlockers` (moved route-local for the Prisma count, pure module keeps the type + `hasLaunchBlockers`); first app→User FKs (plain `String`, UG-1) with the erasure decision recorded in `invitations.md`.

End-to-end invitation lifecycle: tokenised invite, email send, registration, status tracking through to completion.

_Indicative tasks:_

- Invitation creation UI (single + bulk).
- Token generation + opaque URL.
- Email send via Sunrise's email recipes.
- Invitation lifecycle state machine: `pending → sent → opened → registered → started → completed | revoked`.
- Registration flow that links an invitation token to a user account.
- Admin view of invitation states.

### F3.3 — Pre-launch cost estimation

_Status:_ done — merged ([PR #28](https://github.com/human-centric-engineering/conquest/pull/28)) — pure history-aware heuristic estimator + Prisma-free `cost-estimation/` module + read-only `GET …/versions/:vid/cost-estimate` route + shared estimate card in the config editor & invitations page + tests · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F3.1

> Committable tracker: [`planning/features/f3.3.md`](features/f3.3.md). Build decisions (confirmed at planning): **heuristic-only** (no empirical mode — the session engine P4/P6 doesn't exist yet, so there are zero runs to calibrate against; an empirical mode keyed on `AppQuestionnaireTurn` actuals is a future PR); **history-aware token model** (quadratic conversation-history term `HISTORY × Q(Q−1)/2`, not flat-per-question); **`Q` honours the F3.1 caps**; **provider-agnostic pricing via `getModel`**, with **unknown-price ≠ free** (`pricingKnown:false`, USD withheld, never a misleading `$0.00`); **GET-only against persisted config** (no audit, no rate-limit sub-cap); surfaced in the **config editor + invitations page** (one fetch each, `perQuestionnaire = perSession × respondents` scaled client-side).

Pre-launch: estimated tokens × cost per provider × question count + extraction overhead, surfaced to the admin before sending invites.

_Indicative tasks:_

- Estimator that reads Sunrise's model registry for current pricing.
- Per-session and per-questionnaire estimates.
- UI surface in the config editor + launch flow.

### F3.4 — Demo-client invitation branding (DEMO-ONLY)

_Status:_ done — merged ([PR #29](https://github.com/human-centric-engineering/conquest/pull/29)) — themed invites adopted (theming module + `AppDemoClient` theme columns built in P3) · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F2.5.1, F3.2 · _Relocated from the old P2.5._

The first place a prospect sees their brand — the invitation. Snapshots the demo client onto the invitation and themes the invitation email. Can only be built once F3.2's invitation model exists, which is why it lives here and not in P2.5. All `// DEMO-ONLY:`.

> **Open decision RESOLVED at promotion — themed invites.** The prospect's first touch is branded, so the **theming module** (`lib/app/questionnaire/theming/` + the `AppDemoClient` theme columns) is built **now (P3)**, not deferred to P7. F7.1's dependency line stands: the chat surface consumes the already-built module via `themeToCssVariables()` rather than building it.

_Indicative tasks:_

- Denormalise `demoClientId` onto `AppQuestionnaireInvitation` at creation time (O(1) theme lookup; resilient to questionnaire reassignment; indexed).
- _(If themed invites)_ theme columns on `AppDemoClient` + the theming module — `resolveTheme()` (fills nulls with Sunrise defaults) and `themeToCssVariables()`, all `// DEMO-ONLY:`.
- _(If themed invites)_ client-aware invitation email — CTA colour, logo, welcome copy from the resolved theme; Sunrise defaults when null.

**Definition of phase complete.** Admin can configure → estimate cost → invite → see invitation states change as users open the link; demo-client invitations carry their attribution (and brand, if themed invites were adopted).

---

## P4 — Conversational engine (non-streaming)

**Intent.** All the per-turn intelligence — _which question to ask next, how to extract answers from a natural response, how to detect contradictions, when to offer completion_ — exercised as plain functions/capabilities first, without the streaming surface. P6 wraps the streaming layer around this. Six ownerable features; each is unit-testable in isolation.

### F4.1 — Selection strategies

_Status:_ done — merged ([PR #30](https://github.com/human-centric-engineering/conquest/pull/30)) — pure `selection/` registry + `sequential`/`random`/`weighted` deterministic strategies + the full `adaptive` LLM+pgvector strategy behind a sub-flag; `next-question` preview route + `embed-questions` backfill; `AppQuestionSlot.embedding` pgvector column; seeded selection agent · _Size:_ multi-PR (3) · _Owner:_ TBD · _Deps:_ F0.1, F2.2 (tags), F3.1 (config)

Pluggable strategies for picking the next question. `Sequential`, `Random`, `Weighted`, `Adaptive`. Each a unit-tested function. Adaptive is the most complex — embeds the last message, pgvector-ranks unanswered candidates, and an LLM picks the most natural follow-up (fail-soft to `weighted`).

> Committable tracker: [`planning/features/f4.1.md`](features/f4.1.md). Build decisions (confirmed): **`random` added to the `SELECTION_STRATEGIES` enum** (was 3, now 4); **pure strategies over an in-memory `SelectionContext`** (no session tables until F4.6/P6 — strategies are exhaustively unit-testable by hand); **required-before-optional precedence** for random/weighted; **weighted tuning constants in code, not config**; **adaptive is full LLM + pgvector**, gated behind `APP_QUESTIONNAIRES_ADAPTIVE_STRATEGY_ENABLED` (off by default) and **fail-soft to weighted** on any error; **impure deps injected** (`StrategyDeps`) so `lib/` stays Prisma-free; **`embedding vector(1536)` is `Unsupported(...)`** with raw-SQL read/write + HNSW index (create-only + strip migration). API-first via a no-persistence `next-question` preview route. See `.context/app/questionnaire/selection-strategies.md`.

_Indicative tasks:_

- Strategy interface + registry.
- `Sequential`, `Random`, `Weighted` implementations.
- `Adaptive` implementation (largest task — multi-PR).
- Edge cases: no remaining questions, contradictions outstanding, low-confidence slots needing follow-up.

### F4.2 — Answer extraction into slots

_Status:_ done — merged ([PR #32](https://github.com/human-centric-engineering/conquest/pull/32)) — answer-extraction pure core + answer-extractor capability + seeds + `extract-answer` preview route behind a sub-flag · _Size:_ multi-PR · _Owner:_ TBD · _Deps:_ F1.1 (slots), F0.1

Capability that, given a user message + active question + session context, produces `(value, confidence, provenance, rationale, label)` for one or more slots. Side effects on other questions are allowed and recorded.

_Indicative tasks:_

- Extractor capability + system prompt.
- Slot-write path with provenance + confidence.
- Side-effect detection (one message answering multiple questions).
- Synthesised / inferred / direct / refined provenance labelling.

### F4.3 — Contradiction detection

_Status:_ in flight — on `feat/f4.3-contradiction-detection` · _Size:_ 2 PRs · _Owner:_ TBD · _Deps:_ F4.2

> Committable tracker: [`planning/features/f4.3.md`](features/f4.3.md). Build decision (confirmed): **align to the committed F3.1 config schema** — behaviour mode is `off | flag | probe` (`flag` surfaces, `probe` follows up in-conversation), **not** the cadence list this prose originally sketched. **Cadence** (per-turn vs completion-sweep) is built as a pure `shouldRunDetection(mode, windowN, phase)` scheduler — `phase:'turn'` covers _every_turn_ (windowed by `contradictionWindowN`), `phase:'completion-sweep'` covers _sweep_only_ — with **no cadence config column**; `every_n_turns` (a pure cost knob) is deferred to F4.6 where the real turn loop consumes it. Surfaces contradictions to the agent for confirmation; **never auto-overwrites** (resolution is F4.4). Pure core + capability + no-persistence preview route, mirroring F4.2. See `.context/app/questionnaire/contradiction-detection.md`.

_Indicative tasks:_

- Detection capability comparing current answers across slots (LLM, fail-soft).
- Pure mode-aware scheduler (`shouldRunDetection`: per-turn windowed vs sweep-at-completion).
- Surfacing-to-agent contract (`ContradictionFinding` — which slots, why, severity, a `probe` follow-up).

### F4.4 — Answer refinement

_Status:_ in flight — on `feat/f4.4-answer-refinement` · _Size:_ 3 PRs · _Owner:_ TBD · _Deps:_ F4.2

> Committable tracker: [`planning/features/f4.4.md`](features/f4.4.md). Build decisions (confirmed): **persistence is in scope** — F4.4 introduces the answer-persistence foundation (a minimal `AppQuestionnaireSession` + the `AppAnswerSlot` model with `refinementHistory` + migration + a real write path), a **deliberate divergence** from the no-persistence F4.1–F4.3 previews (F4.6 builds its live loop on these same tables); **`overwrite` keeps the original provenance, only a genuine `refine` → `refined`** (the precise "evolved across turns" signal); **own agent + capability** (the LLM decides refine/overwrite/leave) with the _apply_ step a pure deterministic `applyRefinement`; **core-local `REFINEMENT_ACTIONS`/`REFINEMENT_SOURCES` tuples**; **no clock in the pure core** (the persistence seam stamps `createdAt`); **reuse F4.2 `validateAnswerValue`**; **own opt-in sub-flag** (`APP_QUESTIONNAIRES_ANSWER_REFINEMENT_ENABLED`, off by default); the route **persists** against a per-version preview session (`isPreview`, excluded from P8). See `.context/app/questionnaire/answer-refinement.md`.

Allows the agent to update a previous slot's value based on new context, with `refinementHistory` preserved. Used both by contradiction resolution (consuming F4.3's `suggestedProbe`) and by general "user clarified earlier" flows.

_Indicative tasks:_

- Refinement detection (when does the agent decide to refine vs overwrite vs leave alone).
- `refinementHistory` write path on `AppAnswerSlot`.
- Provenance label transition (`direct → refined`).

### F4.5 — Completion logic

_Status:_ in flight — on `feat/f4.5-completion-logic` · _Size:_ 3 PRs · _Owner:_ TBD · _Deps:_ F4.1, F4.2, F4.3, F4.4

Decides when the agent offers submission (based on completion config) and accepts/holds when the user confirms. Drives the contradiction sweep in `sweep_only` mode at the moment of offer.

> Committable tracker: [`planning/features/f4.5.md`](features/f4.5.md). Build decisions (confirmed): **two layers** — eligibility is a deterministic gate (`assessCompletion`, reusing F4.1's coverage helpers via a backward-compatible `CoverageContext` narrowing) and the LLM only **phrases** the offer (a peer capability + agent + sub-flag, the F4.2/F4.3 shape); **required-questions gate blocks the offer** even when weighted coverage clears the threshold (the gate `terminalDecision` lacks); **sweep conflict → hold for review** (never auto-submit over a contradiction; reconcile via F4.4, re-offer); **two routes** — `completion-status` (read-only assess + optional offer) and `complete` (accept/hold, persists status, runs the sweep); maps onto the **committed flat config fields** (no `completionConfig` blob, no `sweep_only` mode) and reuses F4.3's `app_detect_contradictions` + `shouldRunDetection('completion-sweep')`; **no new Prisma model** (builds on F4.4's session/answer tables, adds `markSessionCompleted` to the seam) plus one hardening migration (a raw-SQL partial unique index making the preview-session get-or-create race-safe, drift-probed); the **completion sub-flag does not 404** its route (the free deterministic assessment stays available, only the paid offer phrasing is gated). See `.context/app/questionnaire/completion-logic.md`.

_Indicative tasks:_

- Completion criteria evaluation against `completionConfig`.
- Offer-to-submit logic + agent contract.
- Acceptance path (sweep → submit) and hold path (user wants to keep going).

### F4.6 — Session state machine

_Status:_ in flight — on `feat/f4.6-session-state-machine` · _Size:_ 2 PRs · _Owner:_ TBD · _Deps:_ F4.4 (session/answer persistence), F4.5 (markSessionCompleted seam)

> Committable tracker: [`planning/features/f4.6.md`](features/f4.6.md). Build decisions (confirmed): **deterministic, no LLM** (no capability/agent/sub-flag — a pure transition table + the seam writes, gated by the master flag only); **vocab keeps `active`, adds `paused`** (Option B — the code shipped `active` in F4.4, so an additive `SESSION_STATUSES` edit over a rename, no data-backfill migration, `@default("active")` unchanged); **one thin admin transition route** (`pause | resume | abandon`) — **completion stays the single submit entrypoint** on F4.5's `/complete` route; **status + audit event written atomically** in one `$transaction` so a status never changes without its row; **`markSessionCompleted` generalised** into `transitionSession` (moved to `_lib/sessions.ts`, re-exported from `answer-slots.ts` so the F4.5 route is untouched — completion now writes a `completed` event); **`cost_cap_reached` wired but not fired** (`recordCostCapReached` ready for F6.3/F6.5) and **`created` reserved** for real respondent sessions (F6.1); **`AppQuestionnaireSessionEvent` fully Prisma-modelled** (no raw-SQL object → no drift probe), create-only + stripped migration; **minimal resume state** (`{ status, answeredSlots }` — coverage/next-question stay in the F4.1/F4.5 builders); **no CHANGELOG entry** (app surface, not the Sunrise platform contract). See `.context/app/questionnaire/session-state-machine.md`.

Lifecycle: `active | paused | completed | abandoned` (the plan's `in_progress` is the shipped `active`). Every transition writes an `AppQuestionnaireSessionEvent` row. Used as the audit trail of session-level state.

_Indicative tasks:_

- State transition table + guards.
- Event writes on every transition.
- Resume logic (paused session picks up where it left off).
- Cost-cap-reached event integration (set up here, fired in F6.3 / F6.5).

**Definition of phase complete.** Every per-turn behaviour exercisable by a Vitest integration test driving session state by hand (no chat surface yet). 100% coverage of the selection strategies' edge cases.

---

## P5 — Design-time evaluation (agents-as-judges)

**Intent.** Let an admin evaluate a questionnaire's structure against its stated `goal` and `audience` using Sunrise's existing agents-as-judges infrastructure. Seven judge agents cover distinct dimensions (clarity, coverage, duplicates, type fit, ordering, audience match, goal match). Suggestions land in a review queue; the admin accepts, declines, edits, or applies.

### F5.1 — Judge agents

_Status:_ done — on `feat/f5.1-judge-agents` (2 PRs, one branch): pure `evaluation/` core (dimension registry + judge verdict Zod contract + per-dimension rubric prompts) + seven `kind='judge'` seeds + sub-flag (PR1); `evaluate-structure` capability + no-persistence `evaluate-preview` route (PR2) · _Size:_ 2 PRs · _Owner:_ TBD · _Deps:_ F2.1 (a structure to judge)

> Committable tracker: [`planning/features/f5.1.md`](features/f5.1.md). Build decisions (confirmed at planning): **app-native dispatch, NOT Sunrise's `AiEvaluationRun` worker** — the judges run via one `runStructuredCompletion` call per dimension through a `BaseCapability` (the F4.2–F4.5 shape), emitting **score + actionable findings** rather than the dataset worker's bare 0–1 score, because P5 grades an existing artefact and feeds a suggestion review queue (F5.3), not a case-based subject run (see the [[#Decisions log]] entry, 2026-06-05); **still seeded as `kind='judge'`** (platform Judges surface + agent-resolver/cost reuse) but the **rubric lives in code** (`evaluation/judge-prompt.ts`), the seeded `systemInstructions` a self-describing mirror (the F4.5 split); **`isSystem: false`** app-owned judges (vs. the platform's `isSystem: true`), one seed file with a registry loop; **`processesPii = false`** (admin-authored content, not respondent data); **sub-flag 404s the whole route** when off (no free deterministic half, unlike F4.5); **findings address `targetKey`** (slot `key` / `section:<title>` / `goal` / `audience`), reconciled fail-cleanly by F5.3; **fail-soft per judge**; **no migration** (persistence is F5.2). See `.context/app/questionnaire/design-evaluation.md`.

Seed and tune the seven judges. Each is an `AiAgent.kind = 'judge'` with its own rubric (in code) dispatched app-natively; F5.2 persists runs, F5.3 reviews the findings.

_Indicative tasks:_

- Seven judge seeds — one per dimension (clarity, coverage, duplicates, type fit, ordering, audience match, goal match).
- ~~Grader bindings against Sunrise's grader registry.~~ Superseded — app-native dispatch via the `evaluate-structure` capability, not a grader binding (see tracker).
- Cost-cap and timeout tuning per judge.

### F5.2 — Evaluation run

_Status:_ **done** (tracker: [`features/f5.2.md`](features/f5.2.md)) · _Size:_ ~1 PR · _Owner:_ — · _Deps:_ F5.1

The trigger that fires the seven judges over a version's questions, **persists** the run + its findings (app-owned models, **not** a Sunrise `AiEvaluationRun` link — superseded, see decisions log), and surfaces run history to the admin. Built **synchronous** (no worker/polling): the codebase has no background-task registration seam, so async would force a layering-inverting edit of the platform maintenance tick for no gain over the proven F5.1 dispatch seam. See `.context/app/questionnaire/design-evaluation.md` (§ F5.2 — persisted runs).

_Delivered:_

- `POST /api/v1/app/questionnaires/:id/versions/:vid/evaluations` — runs the panel (shared `runEvaluationPanel`), persists in one transaction, returns the run detail; `GET` (list, newest-first, paginated) + `GET /:runId` (detail with findings).
- `AppQuestionnaireEvaluationRun` + `AppQuestionnaireEvaluationFinding` models (row-per-finding; `dimensionSummary` JSON; minimal finding `status` added now for F5.3); `app_questionnaire_evaluation_run` migration.
- Admin runs sub-page (`…/[id]/evaluations`) + read-only run-detail page, mirroring the `extraction-changes` surface; "Run evaluation" button.
- _Not built (deliberately):_ async worker + status polling — vestigial from the superseded `AiEvaluationRun` vision; runs are synchronous and terminal on return.

### F5.3 — Suggestion review

_Status:_ **done** (tracker: [`features/f5.3.md`](features/f5.3.md)) · _Size:_ ~1 PR · _Owner:_ — · _Deps:_ F5.2

The admin's surface for working through judge suggestions: review queue, accept/decline/edit, apply to the draft version, derive staleness at read time when a suggestion is rendered obsolete by intervening edits. Built on a key divergence from the indicative sketch (confirmed with the user): rather than a UI-only layer over prose findings, F5.3 went **upstream into the F5.1 contract** and gave each finding an optional **structured `proposedEdit`** (a typed op) so Apply is a real one-click write through the existing fork-if-launched seam — the op an _accelerator, never a trust boundary_ (prompt-guided, re-validated at apply). Review state **extends the finding row** (not a separate `AppQuestionnaireSuggestionReview` table — superseded, as F5.2 already added `status` to the row); `stale` is **derived at read time** from a per-run `structureSnapshot`, never stored. See `.context/app/questionnaire/design-evaluation.md` (§ F5.3).

_What shipped:_

- `ProposedEdit` discriminated union (8 ops) on the judge findings contract; soft-degraded on persist; per-dimension prompt guidance.
- Finding review columns + run `structureSnapshot` (additive, nullable migration); staleness-aware run-detail read.
- Apply engine (`_lib/evaluation-apply.ts`) reusing the authoring leaf helpers — validate-before-fork, apply-time staleness re-check, **fork-lineage convergence** (repeated applies from a run reuse one draft instead of re-forking).
- `PATCH …/findings/:findingId` (accept/decline/edit) + `POST …/findings/:findingId/apply` (sub-flag gated, `evaluationApplyLimiter`, 409 with reason on unapplicable).
- Interactive review queue UI (accept/decline/edit/apply per derived `applicable`+`stale`, typed edit-override form, status filter, fork banner).

**Definition of phase complete.** ✅ An admin can request an evaluation, see suggestions per judge, review/edit/apply them, and watch the version evolve. **P5 complete.**

---

## P6 — Conversational session (streaming)

**Intent.** Wrap the P4 engine in a streaming surface. The user sends a message → per-turn orchestrator runs (select → ask | extract → answer → detect | refine | offer completion) → response streams back via SSE. Voice and attachment inputs consume Sunrise's existing primitives verbatim.

### F6.1 — Per-turn orchestrator + streaming

_Status:_ **done** (tracker: [`features/f6.1.md`](features/f6.1.md)) · _Size:_ multi-PR (6 feature PRs) · _Owner:_ — · _Deps:_ F4.1–F4.6

The streaming surface around P4. A **pure** `runTurn(state, invokers)` over session state + user message produces side effects + an agent response, wrapped in an SSE route. Built on key divergences from the indicative sketch (confirmed with the user): **drives the app-native orchestrator, NOT `streamChat`** (which is an LLM-driven tool loop — the wrong shape for a deterministic pipeline; reuses `sseResponse`/`ChatEvent`/cost plumbing only); **three respondent-access scenarios** rather than one — invitation-bound + logged-in-anonymous (both `withAuth`) + a **true no-login anonymous public surface** (HMAC session token, `respondentUserId` null) for pop-up/demo use; **real token streaming for offers** via a plain-prose `provider.chatStream` variant (the F4.5 composer returns JSON); a **dark-launch sub-flag** `APP_QUESTIONNAIRES_LIVE_SESSIONS_ENABLED`. See `.context/app/questionnaire/per-turn-orchestrator.md`.

_Delivered:_ the `AppQuestionnaireTurn` model + `recordTurn` seam (fires `lastUpdatedTurnId`); the pure orchestrator core; authenticated + no-login session-start; the SSE turn loop with real capability invokers + persistence; the streaming offer composer; the HMAC access token + resolver.

_Deferred to follow-ups:_ attachment input (needs extending the F4.2 extraction capability to accept content parts); full `refinementHistory` append on refine persistence; aggregate per-turn token usage on `done`; admin-side anonymous redaction (a later phase); cost-cap firing (F6.3).

### F6.2 — Voice input

_Status:_ **done** (tracker: [`features/f6.2.md`](features/f6.2.md)) · _Size:_ ~1 PR · _Owner:_ — · _Deps:_ F6.1

Voice transcription consumed from Sunrise primitives. Built on one divergence from the indicative sketch (confirmed with the user): rather than folding audio into the SSE `/messages` route, F6.2 adds a **separate transcribe endpoint** `POST /api/v1/app/questionnaire-sessions/:id/transcribe` that returns `{ text, durationMs, language? }` — so P7 can point Sunrise's `<MicButton>` at it **verbatim** (it expects `{ text }`, not a turn stream), the respondent can edit the transcript before sending, and the existing text `/messages` path + SSE loop stay untouched. Transcription reuses `getAudioProvider()` + `provider.transcribe()`, Sunrise's size cap / MIME allowlist constants, the `audioLimiter` (10/min) per-flow sub-cap, and `logCost({ operation: 'transcription' })`. Gated by a **dark-launch sub-flag** `APP_QUESTIONNAIRES_VOICE_INPUT_ENABLED` (seed 022) that opts the paid Whisper path in **on top of** live-sessions — `isVoiceInputEnabled` requires master + live-sessions + voice, since a transcript is only useful once it can be sent through the live `/messages` loop. UI integration of `useVoiceRecording` + `<MicButton>` happens in P7.

_Delivered:_ the transcribe route (reuses `resolveTurnAccess` for all three respondent-access modes); an app-side `validateAudioUpload` (audio + language, no `agentId` — session supplies agent context) reusing the platform constants; the `isVoiceInputEnabled` resolver + `withVoiceInputEnabled` gate; the disabled-by-default sub-flag seed. Audit invariant: no audio bytes or transcript persisted (only the fire-and-forget `logCost`).

### F6.3 — Cost cap enforcement at turn boundary

_Status:_ in flight · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F4.6, F6.1

The per-session cost cap that fires the wrap-up turn at 90% (soft) and halts with 402 + auto-pause at 100% (hard). Both write `AppQuestionnaireSessionEvent` rows. Built on the existing `AppQuestionnaireConfig.costBudgetUsd` field (the indicative `perSessionCostCapUsd` name) — no schema change. Confirmed divergences from the sketch: the soft cap **biases the orchestrator toward offering completion early** (not just a prose hint), and F6.3 also **closes the F6.1-deferred per-turn cost aggregation** (the extract/detect/refine invokers stubbed `0`, so the cap had nothing meaningful to grade) by surfacing each capability's real `costUsd` on its app-owned `data`. Dark-launch sub-flag `APP_QUESTIONNAIRES_COST_CAP_ENABLED` (seed 023), ANDed with master + live-sessions. Tracker: `planning/features/f6.3.md`. Docs: `cost-cap-enforcement.md`.

_Indicative tasks:_

- Pre-turn cost check against the session's `perSessionCostCapUsd`.
- Soft-cap (90%) — agent receives a "wrap up" instruction in its system context for that turn.
- Hard-cap (100%) — turn refused with 402; session auto-paused; event written.
- Tests for both boundaries against scripted costs.

### F6.4 — Demo session reset (DEMO-ONLY)

_Status:_ **done** (tracker: [`features/f6.4.md`](features/f6.4.md)) · _Size:_ ~1 PR · _Owner:_ — · _Deps:_ F4.6, F6.1, F3.1 · _Relocated from the old P2.5._

The between-demos "clean slate" — `POST /api/v1/app/demo-clients/:id/reset-sessions` hard-deletes the session graph for a client's questionnaires so the next prospect starts fresh. Can only exist once that graph does (this phase). Destructive; a production fork removes it — marked `// DEMO-ONLY:`.

_Indicative tasks:_

- Typed-confirmation guard (`confirmSlug` must equal the client slug → `400` on mismatch).
- Transactional delete of sessions + profiles + answer slots + turns + events for every version of every questionnaire with `demoClientId = :id`; optional invitation cleanup behind `?resetInvitations=true` (preserves `started | completed | revoked`).
- Refusals: `409` when any of the client's questionnaires has `anonymousMode` on (too destructive for research-sensitive data); `403` on ownership.
- Returns `deletedCounts`; audit-logged `app_demo_client.reset_sessions` (never deleted from the audit trail).

**Definition of phase complete.** End-to-end conversational session works via API. A scripted client can complete a small questionnaire over SSE; transcripts, costs, audit rows all land correctly.

---

## P7 — User-facing conversational UI

**Intent.** The thing a prospect actually sees. Split-screen layout: streaming chat on one side, answer-slot panel on the other showing live state as the conversation proceeds. Polish to demo-grade. Includes PDF export and the demo-flow E2E test that protects against regressions in the sales-critical happy path.

### F7.1 — Chat surface

_Status:_ not started · _Size:_ multi-PR · _Owner:_ TBD · _Deps:_ F6.1, F6.2, theming module (built in F3.4 if themed invites were adopted, else here)

Live SSE rendering with voice + attachment input wired. Consumes Sunrise's `useVoiceRecording` hook and `<MicButton>` verbatim — F6.2 deliberately shipped a **separate** transcribe endpoint (`POST …/questionnaire-sessions/:id/transcribe` → `{ text, durationMs, language? }`) precisely so `<MicButton>` (which expects an endpoint returning `{ text }`, not a turn stream) can be reused as-is: point its `endpoint` prop at the transcribe route, let `onTranscript` drop the text into the composer (respondent can edit), then send through the existing text `/messages` path. Branding hookup from P2.5 happens here. Includes the demo-flow E2E test (Playwright as an app dev-dep).

_Indicative tasks:_

- SSE message rendering with optimistic local turn append.
- Voice input integration — wire `<MicButton endpoint=…/transcribe>` + `useVoiceRecording` to the F6.2 transcribe route; transcript fills the composer, sent via the text `/messages` path. Gate the affordance on `APP_QUESTIONNAIRES_VOICE_INPUT_ENABLED`.
- Attachment input (consume Sunrise's attachment-input affordance from `AgentTestChat` verbatim).
- Branding render — consume the theming module (built in F3.4, or build it here if invite-theming was deferred); add the tenant-scoped routing + landing/completion theming application points relocated from the old P2.5.
- Playwright setup as an app dev dependency (per [[building-on-sunrise]] — apps own their dev deps).
- Demo-flow happy-path E2E test in Playwright.

### F7.2 — Answer-slot panel

_Status:_ in flight (on `feat/F7.2-answer-slot-panel`) · _Size:_ 2 PRs · _Owner:_ TBD · _Deps:_ F6.1, F7.1

> Committable tracker: [`planning/features/f7.2.md`](features/f7.2.md). Build decisions (confirmed): **read endpoint, not a stream event** (`GET …/questionnaire-sessions/:id/answers`, reusing `resolveTurnAccess` for both respondent kinds; the F6.1 SSE stream emits no slot events, so the panel refetches when each turn settles); **panel scope is an admin config setting** (new `answerSlotPanelScope` config field, `full_progress | answered_only` — `answered_only` filters server-side so pending prompts never reach the client); **click = inline-expand + a confirm-gated Revisit** that re-asks through the same turn loop (no chat-transcript scroll, so `QuestionnaireChat` stays untouched visually); **rationale shown quietly** in the expanded view. `SessionWorkspace` lifts the stream hook so chat + panel share it (an additive `onTurnSettled` drives the refetch; the chat now takes `stream` as a prop). See `.context/app/questionnaire/answer-slot-panel.md`.

Live-updating list of slots with confidence indicators + click-to-jump-to-question. Marked `// DEMO-ONLY:` where it bleeds into questionnaire-specific assumptions so a non-questionnaire fork strips gracefully.

_Indicative tasks:_

- Slot list rendering with live updates from session state.
- Confidence-indicator visual language (quiet, semantic — per the human-centric principle).
- Click-to-jump-to-question interaction.
- Refinement-history disclosure.

### F7.3 — Session lifecycle UX

_Status:_ done (on `feat/F7.3-session-lifecycle-ux`) · _Size:_ 2 PRs · _Owner:_ TBD · _Deps:_ F4.6, F6.3, F7.1, F7.2

> Committable tracker: [`planning/features/f7.3.md`](features/f7.3.md). Build decisions (confirmed): **offer signal = a `GET …/status` read** refetched on the F7.2 `onTurnSettled` (the SSE contract stays closed); **submit gate is upstream** — the offer never appears with a required question outstanding, so submit just re-asserts the offer state via the F4.5 `resolveCompletion` with **no contradiction sweep** (capped-session-always-submits honoured as-is); **pause is signed-in only** (`403 PAUSE_NOT_PERMITTED` for no-login — the anonymous token is client-only). No migration (every status + event exists from F4.6).

Visible session state: pause/resume affordance, completion-offer prompt, submission flow, cost-cap-reached state, anonymous-mode indicator. **The one gap it closed:** before F7.3 a live respondent session had no path to `completed` (the agent offered, nothing recorded acceptance) — the respondent submit route is now that path.

_Indicative tasks:_

- Pause/resume controls + resumption from a saved session.
- Completion-offer UI (agent offers; user accepts or holds).
- Submission flow + confirmation.
- Cost-cap-reached / paused-by-cap UI.
- Anonymous-mode indicator (when the questionnaire is configured that way).

### F7.4 — PDF export

_Status:_ not started · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F8.1 (analytics data shape) or independent

`@react-pdf/renderer` as an app dependency (vertical — not promoted to Sunrise). Admin-facing export of a session's answers first; user-facing PDF download is a nice-to-have.

_Indicative tasks:_

- Add `@react-pdf/renderer` to app `dependencies` (per package.json convention from [[building-on-sunrise]]).
- PDF layout component for a completed session.
- Admin route to download a session's PDF.
- (Optional) user-facing download on session completion.

**Definition of phase complete.** A prospect can complete a branded questionnaire end-to-end on a clean machine; PDF export of results works; demo-flow E2E green in CI.

---

## P8 — Admin analytics, exports, anonymous mode

**Intent.** Make completed sessions readable for the admin. Per-question distributions, completion funnel, cost actuals, exports. Anonymous mode shipped throughout the build is verified and hardened here.

### F8.1 — Admin analytics dashboards

_Status:_ not started · _Size:_ ~1–2 PRs · _Owner:_ TBD · _Deps:_ F4.2 (slots), F4.6 (session events), F3.2 (invitations)

The admin's read-side view of completed-session data: per-question distributions, completion funnel, cost actuals. Tag-aware filtering throughout.

_Indicative tasks:_

- Per-question distribution view (by question type, with tag filtering).
- Completion funnel: invited → opened → started → completed, with drop-off points.
- Per-questionnaire cost dashboard reading `AiCostLog` via Sunrise's existing query helpers.
- Shared filter/scope component across the three views.

### F8.2 — Result exports

_Status:_ not started · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F8.1

CSV + JSON export of session results. CSV is one row per session × question; JSON is the full session graph including provenance + turns. Both respect anonymous mode.

_Indicative tasks:_

- CSV export endpoint + admin UI button.
- JSON export endpoint + admin UI button.
- Anonymous-mode handling in the export pipeline (no PII in either format when configured).

### F8.3 — Anonymous-mode hardening

_Status:_ not started · _Size:_ ~1–2 PRs · _Owner:_ TBD · _Deps:_ F8.1, F8.2 + any surface that touches session data

Verification pass across every surface that touches session data, ensuring no PII leak when `anonymousMode = true`. Flag-gating tightened where needed.

_Indicative tasks:_

- Audit every read path that touches `AppQuestionnaireUserProfile` for anonymous-mode gating.
- Audit exports + analytics + admin UI.
- Integration tests that flip the flag and assert PII absence on every surface.
- Documentation of the anonymous-mode contract.

**Definition of phase complete.** Every analytic surface populated against seeded session data; exports verified for both anonymous and identified modes; integration tests for the anonymous-mode invariants pass.

---

## P9 — Hardening + forking docs

**Intent.** Make the platform demo-grade _and_ fork-ready. Concurrent-session sanity, flag inventory, runbook, the `forking.md` that a real client engagement uses to inherit the platform, and the external-fork upgrade guide for OSS forkers.

### F9.1 — Production hardening

_Status:_ not started · _Size:_ ~1–2 PRs · _Owner:_ TBD · _Deps:_ everything (final pass)

The pre-ship technical hardening: concurrent-session sanity, master + sub-flag inventory, verification that every flag and sub-flag controls the right surfaces independently.

_Indicative tasks:_

- 20+ concurrent-session sanity test: no deadlocks, no orphan turns, no missed audit writes.
- Feature-flag inventory document — master flag + sub-flags (adaptive strategy, voice multipart, eval-auto-run).
- Per-sub-flag verification — with each off, the gated surface is correctly suppressed; rest of platform unaffected.
- Final integration-test pass against the full happy path.

### F9.2 — Operational runbook

_Status:_ not started · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F9.1

`.context/app/questionnaire/runbook.md`: how to spin up a new demo client end-to-end. Road-tested by John or Simon before the phase ships, with friction corrected into the doc.

_Indicative tasks:_

- Draft the "spin up a new demo client" runbook covering tenant creation, branding, content seed, invitation, first session.
- Live road-test by a demo presenter on a clean machine.
- Correct the runbook against road-test friction.

### F9.3 — Forking documentation

_Status:_ not started · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F9.1, F9.2

The fork-readiness deliverables: `forking.md` for an inheriting client-engagement team, the external-fork upgrade guide for OSS forkers, and the consolidated Sunrise-contributions record.

_Indicative tasks:_

- `.context/app/questionnaire/forking.md` — what to keep verbatim, what to rename, what to replace, the rename `sed` recipes, the demo-tenancy replacement procedure.
- External-fork upgrade guide section — git upstream remote, semantic-conflict discipline, contributing fixes via PR. Per [[building-on-sunrise]] / [[fork-readiness-backlog#External-fork upgrade guidance]].
- Sunrise contributions record — what this build contributed back (smaller than originally planned, since v0.0.1 closed the pre-fork backlog; still a real sales artefact).

### F9.4 — Demo content seed (DEMO-ONLY)

_Status:_ not started · _Size:_ ~1 PR · _Owner:_ TBD · _Deps:_ F2.5.1, F7.1 (a complete themeable vertical to seed) · _Relocated from the old P2.5._

The `LOAD_DEMO_CONTENT=1` mechanism that populates a sample demo client + branded questionnaire on a fresh DB. Idempotent; refuses to run without the env var. Strictly demo-only — a fork strips it entirely. Lands **before** F9.2's runbook road-test so the runbook can exercise it.

_Indicative tasks:_

- `prisma/seeds/app-questionnaire/010-demo-content.ts`, idempotent, env-gated.
- Sample demo client + theme + sample questionnaire with realistic content.

**Definition of phase complete.** Platform ships; runbook road-tested; a notional fork can be spun up using only `forking.md`; external-fork upgrade path documented.

---

## How features and tasks work

### Status vocabulary

- **Features:** `not started | in flight | blocked | shipped`. Each feature carries an owner and dependencies. `blocked` lists what's blocking.
- **Tasks (when promoted):** `backlog | available | claimed | in-pr | merged`.

### Indicative vs promoted tasks

Each feature lists **indicative tasks** — a planning aid that sketches the scope, named at the level of "a thing that becomes one small PR." Indicative tasks are _not commitments_. They're an at-a-glance read of how big a feature is and what the rough shape might be.

A task becomes **promoted** when the owner is ready to share that state with the team — they declare it with a stable ID (`T<feature>.<n>`), files-likely-to-touch, dependencies, status. Promotion mirrors the Hub's deliberate "this is real work now" gesture: nothing the rest of the team sees is implicit; everything is something the owner explicitly committed to.

Reality check expected: indicative tasks will reshape when promoted. That's the point of leaving them indicative.

Promoted-task format under a feature in flight:

```
| ID | Task | Files | Deps | Status | PR |
|---|---|---|---|---|---|
| T4.1.1 | Sequential strategy + tests | lib/app/questionnaire/selection/sequential.ts | — | available | — |
| T4.1.2 | Adaptive strategy | lib/app/questionnaire/selection/adaptive.ts | T4.1.1 | backlog | — |
```

### Two feature shapes (PR sizing)

The base model above — _a Feature is a coherent multi-PR capability; a Task is a PR-sized work unit_ — fits the **keystone** features but over-applies to the **contained** ones. F0.1 made this concrete: framed as a multi-PR feature, in practice it was **one PR** and each task was **commit-sized**. Each feature heading carries a `_Size:_` hint to set the right default at promotion:

- **`~1 PR`** — contained, additive, lower-risk (scaffolding, seeds, CRUD, config panels, docs, "consume a Sunrise primitive"). Promote the tasks as **commits in one PR** and batch the review gates once. This is the F0.1 shape, and the most common.
- **`multi-PR`** — keystone features spanning many files/concerns with real risk (ingestion, the per-turn orchestrator, selection strategies, authoring, the chat surface). Here task ≈ PR, as the base model intends; cluster the small tasks into PRs and split out the genuinely large one (e.g. F4.1's `Adaptive`).
- **`~1–2 PRs`** — in between; firms up at promotion.

The hint is a default, not a commitment — packaging is decided per-feature at promotion. The lever is **commits ≠ PRs**: don't reflexively make a keystone one PR (it becomes unreviewable), and don't split a contained feature into many tiny PRs (each re-runs the gates).

> **The hints aren't platform-aware audits.** They estimate net-new _app_ code but were sized from the task list, not a per-feature check of what Sunrise already provides — so wherever the platform pre-bakes the machinery (e.g. invitations: token generation, email, and registration all ship in Sunrise — `.context/admin/invitations.md`), the real size is smaller. The definitive sizing happens at **promotion**, when the feature is designed against Sunrise's actual capabilities the way F0.1 was — and some hints will shrink once a planned "build X" reduces to configuring a workflow or consuming a primitive.

**Sizing against the gate-suite tax — the dominant force (F1.1 calibration).**

The lever above ("commits ≠ PRs") is right but underspecified on _which way_ to lean. F1.1 settled it with real data: **the fixed cost you amortise per PR is the whole gate suite — `/pre-pr` + `/security-review` + `/test-review` + `/code-review` — plus CI minutes, not just CI.** Four heavyweight multi-agent passes per PR. That tax is large enough that the default should be **the fewest PRs that each still get one honest review pass**, not the finest cut the seams allow.

- **Floor test (operational):** if a capable session writes the code faster than the gate suite runs, the PR is below the floor — bundle it up. The work should clearly exceed the ceremony.
- **A risk-type seam is a split you must _justify_, not a default.** The reviews run over the whole diff and are thorough enough to cover mixed concerns in one PR, so "the LLM path and the HTTP path want different reviewers" is a _weak_ reason to split — set against paying the entire suite (and CI) a second time. Split only when the isolated slice is **substantial _and_ carries a genuinely distinct high-stakes concern** (a live endpoint's security surface is the canonical one).
- **Ceiling unchanged:** stop bundling when a PR can no longer get one coherent review pass, or a security-critical surface would be buried in a large unrelated diff.

F1.1 (keystone) was planned as four risk-typed PRs; in practice **PR1 (schema) was minutes of work — far below the floor — so it folded into PR2**, and PR3/PR4 (#14, #15: ~1.4k and ~2.3k diff) sat right at the floor and could defensibly have been one. The honest cut was **~2 PRs — "the engine" (schema + pure core + capability + seeds, all static / unit-tested) and "the live endpoint" (route + persistence + integration tests, where `/security-review` earns its run)** — not four. Treat the per-feature PR count as "fewest that stay reviewable," and let the executor merge seams at the moment of work.

### Asking Claude to plan a feature or task

When you're ready to work on something, point Claude at it by ID:

> "Let's plan **F4.1** — selection strategies. Read the project plan and the related sections of the original phases doc (kept as reference), then propose how we approach it."

Claude reads this doc for intent and the original [[Conversational Questionnaire Phases]] for detail if needed, then produces an implementation plan for review. The plan itself stays high-level; deep prescription only happens at the moment of work.

---

## Decisions log

Append-only. Newest at the top. Each entry: date, decision, context, link.

- **2026-06-07 — Deferred-item audit + gap-fills (`feat/close-deferred-gaps`).** An audit against the merged code (PRs #10–#48) found the plan header was stale (it said "P6 in progress / P7 not started" when both phases were fully merged) and surfaced **five items that were deferred to a home feature which has since shipped, but were never actually delivered** — the same shape as the admin upload-questionnaire UI (#48), which had fallen between F1.1 (API-only) and the P2/P7 UI work with no owning feature. All five verified against the code, not the trackers. **(1) Invite claim-via-existing-login** — F3.2 returns `409 ACCOUNT_EXISTS`; claim-via-login was deferred to P7 (done) and never built. **(2) Live `refinementHistory` append** — F4.4 built the model; F6.1 deferred wiring `persistRefinement` into the live turn loop (only the F4.4 preview route appends), so real sessions silently drop the history trail P8 will read. **(3) Clone-for-client admin utility** — parked at "promote once F2.2 + F3.1 exist" (done since P3); never built. **(4) Attachment input** — an F7.1 indicative task that shipped out-of-scope, blocked on an F4.2 content-parts extension that was itself deferred in F6.1; voice was wired (F6.2/F7.1) but attachments never were. **(5) `every_n_turns` contradiction cadence** — a cost knob deferred to "F4.6's real loop" (F6.1); detection still runs every turn. **Also delivered with this branch: the demo-client branding preview** — the admin sets four theme fields (CTA/accent colour, logo, welcome copy) but the admin UI never showed them back; a new `DemoClientThemePreview` (reuses `resolveTheme()` + the escaped `--app-logo-url` background) now renders swatches/logo on the demo-clients list, detail page, and live in the edit form. The five gap-fills land as one-PR-each commits on the branch, ordered by priority (correctness 1–2 before P8). Each gets a `planning/features/fX.Y.md` tracker as it ships. (These are app-internal deferrals, not Sunrise-platform gaps, so they're tracked here in the decisions log — not in `upstream-gaps.md`, which is platform-scoped.)

- **2026-06-06 — F7.3 session lifecycle UX (on `feat/F7.3-session-lifecycle-ux`).** The respondent's window onto the session lifecycle: pause/resume, the completion-offer → submit flow, the cost-cap state, and the anonymous-mode indicator. 2 PRs. **The gap it closed:** a live respondent session had **no path to `completed`** — the orchestrator streamed a completion offer ("Would you like to submit?") as prose but nothing recorded acceptance (the only `completed` transition was the admin-only `/complete` preview route); F7.3's respondent `submit` route is now that path. **Decisions (confirmed with the user):** **offer signal = a `GET …/status` read, not a stream frame** — the F6.1 SSE stream carries no offer marker, so rather than reopen that contract (the F7.2 precedent) a new status endpoint returns a respondent-safe `SessionStatusView` (completion kind + coverage + a coarse cost **tier**, never raw spend + `anonymous`) and the UI refetches it on the same `onTurnSettled` that drives the answer panel; the Submit affordance shows when `completion.kind === 'offer'`. **Submit gate is upstream** — `assessCompletion` only returns `offer` once the required-questions gate is clear, so the Submit affordance never appears with a required question blank; the submit route just re-asserts the offer state via the F4.5 `resolveCompletion('accept', …, { run: false })` with **no contradiction sweep** (contradictions surface live via F4.3 during chat), the lone exception being the existing "a question-cap-reached session can always submit" behaviour, honoured as-is. **Pause is signed-in only** — a no-login session's token is client-only (lost on reload), so a deliberate pause has nowhere durable to resume from; the `lifecycle` route refuses anonymous callers `403 PAUSE_NOT_PERMITTED` (they still see system states), and `canPause`/`canResume` mirror that in the UI (a budget-paused `hard`-tier session is non-resumable). **Backend (PR1):** pure `session/status-view.ts` (`buildSessionStatusView` + `canSubmitSession`); `_lib/session-status.ts` reuses `buildTurnContext` so the assessment is byte-identical to the live turn's; three routes `GET status` / `POST lifecycle {pause|resume}` / `POST submit` under `questionnaire-sessions/[id]/`, all reusing `resolveTurnAccess`, gate order flag→load→access→action, illegal transition→409; registry entries. **Frontend (PR2):** stream hook gains `completed` (+ `BLOCKING_STATUSES`) and an imperative `applyStatus`; a new `useSessionLifecycle` hook (status view + pause/resume/submit + derived flags) lifted into `SessionWorkspace` alongside the F7.2 stream+panel hooks (the shared `onTurnSettled` now refetches both panel and status); `lifecycle/` components `SessionLifecycleBar` (anon badge, pause/resume, soft-cost hint — renders nothing when empty), `CompletionOffer` (Submit CTA + dismiss), `SessionComplete` (themed confirmation replacing the workspace on `completed`); authed page SSR-seeds the status view and maps it to the initial chat status (budget-pause→`cost_capped`, respondent pause→resumable `not_active`, completed→confirmation), anon boot fetches on mount. **No migration** (every status + event exists from F4.6); gated entirely by the existing `APP_QUESTIONNAIRES_LIVE_SESSIONS_ENABLED`. **Tests:** pure builder unit; the three routes at integration (gate order, both access modes, anon-pause 403, submit idempotency/409s/happy path, illegal-transition→409); `use-session-lifecycle` + `applyStatus` hook tests; lifecycle component tests. `npm run validate` green. **No CHANGELOG entry** (app surface). Tracker: `planning/features/f7.3.md`. Docs: new `session-lifecycle.md` + updated `per-turn-orchestrator.md` / `answer-slot-panel.md`.

- **2026-06-06 — F7.2 answer-slot panel (on `feat/F7.2-answer-slot-panel`).** The live panel beside the F7.1 chat showing answer slots as the conversation fills them in — confidence, provenance, refinement history, per-slot Revisit. 2 PRs. **Decisions (confirmed with the user):** **read endpoint, not a stream event** — the F6.1 SSE stream emits only `start/content/warning/done/error` (answers persist server-side before `done`), so rather than extend that contract the panel reads a new `GET /api/v1/app/questionnaire-sessions/:id/answers` and refetches when each turn settles; the route reuses `resolveTurnAccess` (authed owner OR anonymous `X-Session-Token`) so it serves both respondent kinds, gate order flag→load→access, no status gate (paused/completed still show answers), no extra limiter (read). **Panel scope is an admin config setting** — new per-version `answerSlotPanelScope` (`full_progress | answered_only`, default `full_progress`) added via the F3.1 7-step config path; `answered_only` filters in the **pure builder** (`buildAnswerPanelView`) so pending prompts are never sent to the client; `totalCount` still reflects the whole version so the header reads "N captured" honestly. **Click = inline-expand + a confirm-gated Revisit** that re-asks through the same turn loop (a true scroll-to-turn was rejected — it would force touching `QuestionnaireChat` and has a turn-ordinal alignment hazard). **Rationale shown quietly** in the expanded view. **Live wiring:** a new `SessionWorkspace` client parent lifts the single `useQuestionnaireSessionStream` so chat + panel share it — an additive `onTurnSettled` option (fires on a clean settle to `idle`) drives `panel.refetch`, and the shared stream is what lets the panel's Revisit call `stream.sendMessage(...)`; `QuestionnaireChat` was refactored to **take `stream` as a prop** (hook moved up), render otherwise unchanged. **Layout:** both pages render `SessionWorkspace` inside the existing `BrandThemeProvider` (panel inherits brand vars), `lg:grid-cols-[1fr_22rem]`, panel `hidden lg:flex` so `< lg` keeps the full-width F7.1 chat; authed page SSR-seeds the panel (`loadAnswerPanelState`), the anonymous page can't (client-only token) so it skeletons briefly. **Migration** `…_add_config_answer_panel_scope` (one ADD COLUMN, create-only + phantom pgvector DDL stripped, drift 11/11). **Pure core** (`lib/app/questionnaire/panel/{types,answer-panel,confidence}.ts`) Prisma-free; the `_lib/answer-panel.ts` seam does one query incl. a turn-id→ordinal map for `answeredAtTurnIndex`; authoring internals (`weight`, tags, config) never projected. Confidence reads as a quiet semantic dot (high/moderate/low/unscored, the admin eval-chip thresholds), never a raw number. **Tests:** pure builder + confidence bands (unit); the answers route (gate order incl. flag-before-load, both access modes, scope projection, no-leak) at integration; panel/indicator/provenance/history component tests + the `use-answer-panel` hook + the `onTurnSettled` wiring; schema-shape + config-schema extended. `npm run validate` green; 3861 affected tests pass. **No CHANGELOG entry** (app surface). Tracker: `planning/features/f7.2.md`. Docs: `answer-slot-panel.md` + `configuration.md`.

- **2026-06-05 — P5 judges dispatch app-natively, not through Sunrise's `AiEvaluationRun` worker.** The plan's F5.2 sketch said the evaluation run "persists the link to the resulting Sunrise `AiEvaluationRun`", implying P5 reuses the dataset-driven evaluation pipeline. At F5.1 build we reframed: that worker (`lib/orchestration/evaluations/run-worker.ts`) grades a _subject agent/workflow's generated output over a dataset of cases_ and scores each 0–1 — but P5 grades an **artefact that already exists** (the authored questionnaire structure) and must emit **actionable suggestions** for a review queue (F5.3), not bare scores. Forcing the dataset model would need a synthetic dataset + a passthrough subject, and score-only judges wouldn't produce the suggestion rows F5.3 consumes. So the seven judges are dispatched the way every P4 capability already is — one `runStructuredCompletion` call per dimension through a `BaseCapability` (`evaluate-structure`) — returning `{ score, findings }`. They're **still seeded as `kind='judge'` agents** (so they surface in the platform Judges UI and reuse the agent resolver / cost / admin-edit machinery), but the load-bearing rubric lives in code (`evaluation/judge-prompt.ts`), the F4.5 completion-agent split. F5.2 builds app-owned run + suggestion models on this dispatch seam rather than linking an `AiEvaluationRun`. Consequences: F5.2's `AppQuestionnaireEvaluationLink`-to-`AiEvaluationRun` line is superseded; the "grader bindings against Sunrise's grader registry" indicative task in F5.1 is dropped. Reference: [[features/f5.1]] · `.context/app/questionnaire/design-evaluation.md`.
- **2026-06-03 — P2.5 decomposed: demo tenancy is a cross-cutting concern, not a phase.** The decimal was the tell — P2.5 was inserted between P2 and P3 without the dependency pass the round-numbered phases got. Most of it can only be built once the model each piece hangs off exists (invitation branding needs P3's invitation model; session reset needs P4/P6's session graph; the theming render points need P7's user pages; the content seed needs a complete vertical) — and the original F2.5.1 even listed `AppQuestionnaireSession` scoping that P4/P6 hasn't built yet. P2.5 now keeps only the **foundation** (F2.5.1 — demo-client identity table + `AppQuestionnaire` FK + admin attribution), the one slice that must lead so P3+ build tenant-aware in a single pass. The rest is distributed into its host phase as a marked sub-feature: invitation branding → **F3.4**, demo session reset → **F6.4**, theming render + tenant routing → **F7.1/F7.3**, clone-for-client → **P3+** (after F2.2 + F3.1), demo content seed → **F9.4**. **Fork-strippability is preserved by keeping every piece in the same marked modules** (`lib/app/questionnaire/theming/`, the `demo-clients/` dirs, `// DEMO-ONLY:` headers): _decompose the schedule, preserve the module boundary_ — a fork still strips demo tenancy in one grep sweep. One open decision deferred to F3.4 promotion: themed vs plain invitation emails determines whether the theming module is built in P3 or P7.
- **2026-06-02 — Version status transitions (launch/archive) pulled into F2.1 PR2.** The original phasing parked launching in P3 (alongside invitations + cost estimation), which would have left `forkVersionIfLaunched` dormant — nothing could set a version to `launched`, so the fork branch was untestable end-to-end. Bringing the lifecycle status flip forward into PR2 (a `PATCH …/versions/:vid/status` with a minimal launch guard — goal + ≥1 section + ≥1 question) makes the fork seam **live**: editing a launched version actually forks a new draft, exercised by tests and the editor. The _full_ config-completeness launch gate (audience, config, pre-launch cost estimate) and the invitation/registration machinery remain **F3.1/P3**; `countLaunchBlockers` stays a zero-returning P3/P4 seam. Two further PR2 decisions: manual structural edits are audited via `logAdminAction` only (the `AppQuestionnaireExtractionChange` log stays the _extractor's_ provenance, F2.3's consumer) and a fork starts a clean editorial lineage (structure copied, change records not); reorder uses `@dnd-kit` (app dependency). Reference: [[features/f2.1]].
- **2026-06-01 — App changes are tracked in the planning docs, not `CHANGELOG.md`.** `CHANGELOG.md` is the **Sunrise platform** changelog (the public-surface record forks consume); ConQuest's own app surface is not added there — it would be miscategorised noise that conflicts on every upstream sync. The app's record of _what's done / deferred / decided_ lives in these planning docs and is the reference for planning the next feature: **done** → [[#Work completed to date]] plus the per-feature trackers under `features/`; **deferred / platform gaps** → `planning/upstream-gaps.md`; **decided** → this log. A consumer-facing changelog for forks of ConQuest _itself_ is a separate, later decision.
- **2026-06-01 — App docs consolidated under `.context/app/`.** Replaced the `.context/application/` outlier with a single app-docs root, `.context/app/`, using namespace subfolders that mirror the substrate pattern and the code's app tier (`lib/app/`, `app/api/v1/app/`, `prisma/schema/app-*`). `planning/` holds this development plan, the `features/` trackers, and the forward-looking `upstream-gaps.md` ledger (sibling to this Decisions log and the Carried Sunrise patches section); `questionnaire/` holds the domain/technical docs (overview, schema, development). Purely an app-owned reorg — Sunrise's substrate never referenced `.context/application/`.
- **2026-05-31 — Demo-client tenancy is a branding partition, not real isolation.** Sunrise ships single-tenant by default with zero tenancy machinery (no `Org` table, no `orgId`, no RLS) — multi-tenancy is a fork-level RLS retrofit at the `lib/db/client.ts` chokepoint, gated behind `TENANCY_MODE=multi`, _not_ a switch. The plan does **not** require multi-tenant Sunrise; it runs on standard single-tenant Sunrise. P2.5's tenancy (F2.5.1) is a deliberately light, app-owned branding/content partition with application-layer scoping — adequate because demo clients aren't adversarial. A fork into a real multi-customer product activates Sunrise's RLS seam rather than promoting the demo table into a security boundary (which is the trap Sunrise's multi-tenancy doc explicitly warns against). Reference: Sunrise `.context/architecture/multi-tenancy.md`.
- **2026-05-30 — Replaced the phased prompt document with this plan.** The original `Conversational Questionnaire Phases.md` is preserved as reference detail (deep specifications per phase remain useful when planning a feature), but the working source-of-truth is this lighter, intent-shaped doc. Rationale: the prompt-per-phase format was overkill, premature, and didn't leave space for in-flight reframes.
- **2026-05-30 — Sunrise pre-fork backlog closed in v0.0.1.** Multi-file Prisma schema, recursive seed discovery, capability registration hook, admin nav registry, ESLint app-boundary, user-FK pattern, app env-var extension are all in. Phases that previously workaround these are simplified. Reference: [[fork-readiness-backlog]].
- **2026-05-30 — Apply the [[building-on-sunrise]] model.** Fix-in-place, classify, promote-upstream for generic seams; carry app-specific changes locally and track them. Supersedes the universal zero-touch framing in the original phases doc.

---

## Work completed to date

Append-only. Newest at the top.

> Backfill note (2026-06-06): entries for F4.3 through F6.2 were reconstructed in one pass from their per-feature trackers (`features/f*.md`) + git history — the aggregate log had lagged since F4.2 while the per-feature trackers stayed current. They're accurate to the trackers but less granular than the first-hand entries above F4.2; the trackers remain the fuller record.

- **2026-06-06 — F6.3 cost cap enforcement ([PR #42](https://github.com/human-centric-engineering/conquest/pull/42)).** Puts a ceiling on respondent LLM spend: the per-session USD budget (`AppQuestionnaireConfig.costBudgetUsd`) enforced at the live `/messages` turn boundary, firing the `cost_cap_reached` hook F4.6 wired but never fired. **No migration** (reuses the existing field; the indicative `perSessionCostCapUsd` is `costBudgetUsd`). **Decisions (confirmed):** **include the cost-accuracy wiring** — the recorded per-turn `costUsd` only captured offer-stream (+ adaptive) cost because the extract/detect/refine invokers stubbed `0`, so a cap had nothing meaningful to grade; F6.3 closes the F6.1-deferred per-turn aggregation by surfacing each capability's real `costUsd` on its app-owned `data` (from the `runStructuredCompletion` result already logged to `AiCostLog`), invokers read it, the orchestrator sums it — **no platform `CapabilityResult` change**; **soft cap biases toward offering early** (not just a prose hint) — at ≥90% a `not_ready` assessment with ≥1 answered is responded to as an offer (never bypassing the `blocked_on_required` gate, never on an empty session) **and** the offer prose carries a wrap-up instruction; **dedicated dark-launch sub-flag** `APP_QUESTIONNAIRES_COST_CAP_ENABLED` (seed 023, off by default), ANDed with master + live-sessions via `isCostCapEnforcementEnabled`; **turn-boundary semantics** — "spend so far" is `sumSessionTurnCost` (`_sum` of prior `AppQuestionnaireTurn.costUsd`), the current turn's cost recorded afterward and caught next boundary; **hard cap is pre-stream** — 402 `COST_CAP_REACHED` + `pauseSession` + a `cost_cap_reached` (`tier:'hard'`) event before any turn work, then the status gate locks every later turn; **soft event fires once** (deduped by `hasCostCapReachedEvent`). **Delivered:** the pure `classifyCostCap` (`session/cost-cap.ts`); `sumSessionTurnCost` + `hasCostCapReachedEvent` seams + `recordCostCapReached` `tier` metadata; the flag/resolver/seed; `TurnState.costPressure` + `OfferComposeInput.costWrapUp` threaded through the orchestrator + offer composer; the route pre-stream gate. **Tests:** classifier boundaries + orchestrator soft-bias (unit); capability + invoker cost surfacing, `sumSessionTurnCost`, event tier/dedupe, offer wrap-up prose, and the `/messages` matrix (null-cap/below/hard-402+pause/soft-once+biased-offer/dedupe/flag-off) at integration — full session suite green (914). **No CHANGELOG entry** (app surface). Tracker: `planning/features/f6.3.md`. Docs: `cost-cap-enforcement.md`. **Gates green:** `/pre-pr` (changed-file coverage 88–100%, anti-patterns clean), `/code-review high` (2 robustness fixes — hard-cap pause-before-record ordering, soft-cap best-effort audit write), `/test-review` (4 findings ≥80 actioned — symmetric capability cost-surfacing assertions, ordering pin, mock fidelity), `/security-review` (no findings).
- **2026-06-06 — F6.2 voice input (on `feat/f6.2-voice-input`).** Lets a respondent answer by voice. **Divergence from the indicative sketch (confirmed with the user):** rather than folding audio into the streaming `/messages` route, ships a **separate transcribe endpoint** `POST …/questionnaire-sessions/:id/transcribe` → `{ text, durationMs, language? }`, so P7 can point Sunrise's `<MicButton>` (which expects an endpoint returning `{ text }`, not a turn stream) at it verbatim and the respondent can edit before sending. Reuses Sunrise primitives wholesale: `getAudioProvider()` + `provider.transcribe()` (OpenAI Whisper), the size cap / MIME-allowlist constants + `enforceContentLengthCap` from `lib/validations/transcribe.ts`, the `audioLimiter` (10/min, keyed `audio:qn:<rateKey>`), `logCost({ operation:'transcription' })`, and `resolveTurnAccess` (all three F6.1 access modes). One app-side addition — `_lib/audio-upload.ts` `validateAudioUpload` (the platform's `validateTranscribeUpload` minus its mandatory `agentId`, since the session supplies agent context), reusing the platform constants so formats/limits can't drift. Gated by a new dark-launch sub-flag `APP_QUESTIONNAIRES_VOICE_INPUT_ENABLED` (seed 022, off by default) + `isVoiceInputEnabled`/`withVoiceInputEnabled` — `isVoiceInputEnabled` requires master + live-sessions + voice (voice depends on the live surface it feeds; tightened during `/code-review`). **No migration** (feature flags are `featureFlag` rows). **Audit invariant:** no audio bytes or transcript persisted — the only happy-path write is the fire-and-forget cost row. **Tests:** 28 route integration (gate order flag→session→access→status→audio sub-cap→size guard→multipart→validation→provider; the live-sessions-prerequisite 404; all three access modes; the audio-validation matrix; 503/502 + no-message-leak; happy path incl. the `transcription` cost-log with `sessionId` metadata and no `agentId`, language/filename/MIME forwarding; pre-parse size guard; no-audio-persistence). `npm run validate` + full session suite green. Docs: `per-turn-orchestrator.md` (transcribe route + gating), the F7.1 plan note (MicButton wiring). **No CHANGELOG entry** (app surface, not Sunrise platform). Tracker: `planning/features/f6.2.md`. **Gates all green:** `/pre-pr` (clean — coverage 95–100% on new files, no anti-patterns, CHANGELOG N/A), `/test-review` (4 findings ≥80 actioned — envelope `success` pinning + the two 401 codes via an `expectError` helper), `/code-review high` (1 finding actioned — voice gate now requires live-sessions so there's no reachable-but-dead paid Whisper path; + audit-invariant test reference), `/security-review` (pending).
- **2026-06-06 — F6.1 per-turn orchestrator + streaming ([PR #40](https://github.com/human-centric-engineering/conquest/pull/40)).** Opens P6 — wraps the P4 engine in a live streaming surface: a respondent creates a session and drives a streamed turn loop (extract → detect → refine → assess → respond) over SSE, the first feature to mint real respondent sessions + persist turn records. 6 feature PRs on one branch. **Decisions (confirmed):** **drives the app-native orchestrator, NOT `streamChat`** (that's an LLM-driven tool loop — the wrong shape for a deterministic pipeline; reuses `sseResponse`/`ChatEvent`/cost plumbing only); **pure core + injected invokers** (`lib/app/questionnaire/orchestrator/` Prisma-free `runTurn(state, invokers)`, impure capability calls injected at the route seam, per-step flags resolved by the route); **three respondent-access scenarios** — invitation-bound + logged-in-anonymous (both `withAuth`) + a **true no-login anonymous public surface** (stateless HMAC session token, `respondentUserId` null, modelled on `approval-tokens.ts`, NOT a bare cuid) for pop-up/demo use; **real token streaming for offers** via a plain-prose `provider.chatStream` variant (the F4.5 composer returns JSON); a **dark-launch sub-flag** `APP_QUESTIONNAIRES_LIVE_SESSIONS_ENABLED` (seed 021). **Delivered:** the `AppQuestionnaireTurn` model + `recordTurn` seam (fires `lastUpdatedTurnId`); the pure orchestrator core (19-case branch matrix); authenticated + no-login session-start; the SSE turn loop with real capability invokers + persistence; the streaming offer composer (`offer-stream.ts`); the HMAC access token + `resolveTurnAccess`. **Deferred:** attachment input; full `refinementHistory` append on refine persistence; aggregate per-turn token usage on `done`; admin-side anonymous redaction; cost-cap firing (F6.3). **Tests:** orchestrator branch matrix (unit); turn-context mapping, turn-run persistence, offer streaming, session create (invite/anon), access resolver, token round-trip, and both routes (gate order + SSE framing + persistence) at integration. **No CHANGELOG entry** (app surface). Tracker: `planning/features/f6.1.md`. Docs: `per-turn-orchestrator.md`. Gates green: `/pre-pr`, `/code-review` (detect-floor / `selectionRound` / API-key-parity fixes), `/test-review` (11 findings ≥80 actioned).
- **2026-06-05 — F5.3 design-time suggestion review ([PR #39](https://github.com/human-centric-engineering/conquest/pull/39)).** Closes P5 — turns F5.2's persisted judge findings into an interactive **review queue**: accept / decline / edit / **apply** a suggestion to the draft version (forking a launched version first, like every authoring edit), with obsolete suggestions derived **stale** at read time. **Load-bearing decision (user chose quality over PR size):** went _upstream_ into the F5.1 findings contract and gave each finding an optional **structured, executable `proposedEdit`** (a `PROPOSED_EDIT_OPS` discriminated union) so a suggestion can be one-click-applied through the existing fork-if-launched seam — a human always confirming. **Guardrails:** `proposedEdit` is an accelerator, never a trust boundary — prompt-guided not provider-enforced (plain prompt + Zod parse), soft-degraded to `null` on malform at persist (`coerceProposedEdit`), and **re-validated at apply time exactly like a hand authoring edit** (`validateTypeConfig`, target resolution, ordinal bounds); reuses the authoring leaf helpers (not the F2.1 HTTP handlers) for zero blast radius; `accept` (triage) and `apply` (mutate/fork) stay separate; `stale` is **derived, never stored**, via a per-run `structureSnapshot` diffed against the targeted slice (so an unrelated edit never falsely stales a finding); fork-lineage convergence reuses a prior apply's draft instead of re-forking. **Schema:** migration `20260605213442_app_evaluation_finding_review` (create-only, phantom DDL stripped) — additive nullable columns on the finding (`proposedEdit`, `editedOverride`, `decidedByUserId`, `decidedAt`, `appliedAt`, `appliedToVersionId`) + run `structureSnapshot`, no backfill. **Routes:** `PATCH …/findings/:id` (accept/decline/edit) + `POST …/findings/:id/apply` (sub-flag-gated, `evaluationApplyLimiter` 60/min, `applied` terminal → 409, unapplicable → 409 with reason); the run-detail component is now the interactive queue. **Known limits (designed-for):** `add_question`/prose-only route to the editor (deep-link/manual); cross-fork content-staleness best-effort; no bulk apply. **Tests:** `ProposedEdit` schema + `coerceProposedEdit`, the staleness deriver (incl. unrelated-edit-not-stale), the apply engine (early returns, `target_gone`, in-place apply, fork-lineage convergence), the review + apply routes (gating, scope, terminal 409, edit override, unapplicable 409, fork meta). **No CHANGELOG entry** (app surface). Tracker: `planning/features/f5.3.md`. Docs: `design-evaluation.md` (F5.1–F5.3).
- **2026-06-05 — F5.2 evaluation run persistence ([PR #38](https://github.com/human-centric-engineering/conquest/pull/38)).** Turns F5.1's ephemeral judge **preview** into a **persisted, synchronous run**: a POST runs the seven-judge panel over a version's structure, writes the run header + one row per finding in a single transaction, returns the completed run, and adds an admin run history (list + read-only detail) — the rows F5.3 mutates. **Decisions (confirmed):** **synchronous run — no worker/polling** (async explicitly rejected: the codebase has no app-level background-task seam — the maintenance tick is a platform-owned hardcoded array — so a worker would force a layering-inverting platform edit for no gain; `status` is a plain String holding a terminal value so a future worker adds `running`/`queued` with no migration); **app-owned models, NOT a Sunrise `AiEvaluationRun` link** (`AppQuestionnaireEvaluationRun` + `AppQuestionnaireEvaluationFinding` on the F5.1 app-native dispatch seam — the dev-plan's `AiEvaluationRun` sketch is superseded); **row-per-finding, not a JSON blob** (F5.3's accept/decline/edit/apply + filter/paginate wants rows; the run header keeps a small ≤7-entry `dimensionSummary` JSON read wholesale); **minimal finding `status` added now** (default `pending`) so F5.3 extends rows; **shared dispatch service** `runEvaluationPanel` extracted (Prisma-free) so the preview and run routes share one fail-soft fan-out; **POST sub-flag-gated, GETs read-only under the master flag** (history stays readable if the sub-feature is later disabled); reuses `designEvaluationLimiter` (20/min). **Migration** via `--create-only`, phantom platform DDL hand-stripped, drift-check 11/11. **UI:** runs table + read-only detail grouped by dimension, sub-flag-gated Evaluations entry. **Tests:** schema-shape (both models, the six indexes, both CASCADE FKs, no-platform-DDL guard), `run-panel` unit (fan-out + the three fail-soft paths), route integration (POST gate order + derived `completed`/`partial`/`failed` status + finding rows; GET list + detail). **No CHANGELOG entry** (app surface). Tracker: `planning/features/f5.2.md`. Docs: `design-evaluation.md` § F5.2.
- **2026-06-05 — F5.1 design-time evaluation judge agents ([PR #37](https://github.com/human-centric-engineering/conquest/pull/37)).** Opens P5 — stands up **seven `kind='judge'` agents** that score a version's structure against its stated goal/audience across distinct dimensions (clarity, coverage, duplicates, type fit, ordering, audience match, goal match), each emitting a per-dimension score **+ actionable findings**; ships the judges, a dispatch capability, and a **no-persistence preview route**. **Decisions (confirmed):** **app-native dispatch, NOT Sunrise's `AiEvaluationRun` worker** (that grades a subject's generated output over a dataset and scores 0–1; P5 grades an existing artefact and needs suggestions — so judges dispatch like F4.2–F4.5, one `runStructuredCompletion` per dimension); **seeded `kind='judge'` but rubric lives in code** (`evaluation/judge-prompt.ts`, the seeded `systemInstructions` a self-describing mirror; tuning is a git diff); **`isSystem: false`** (app-owned, editable, in backup/export; re-seed never clobbers operator edits); **one seed file iterating the dimension registry** (the panel is a set); **`processesPii: false`** (admin-authored content, not respondent data); **sub-flag 404s the whole route when off** (the entire route is paid LLM work — no free half, the F4.2 posture); **findings address targets by `targetKey`** (slot key / `section:<title>` / `goal` / `audience`, reconciled fail-cleanly by F5.3 at apply time); **fail-soft per judge** (one errors → diagnostic, the other six return; zero judges seeded → 404). **No migration, no persistence** (F5.2 adds the models). Sub-flag `APP_QUESTIONNAIRES_DESIGN_EVALUATION_ENABLED` (seed 019) + `isDesignEvaluationEnabled`; seeds `018-design-evaluation-judges` (7) + `020-design-evaluation-capability`; `designEvaluationLimiter` (20/min). **Tests (51):** unit (dimension parity, judge-schema incl. score bounds + findings cap + JSON-schema, judge-prompt structure/rubric splice/serialisation); capability integration (score+findings, dimension stamp, reasoning tier, cost-as-CHAT, retry repair, fail-closed); route integration (gate order, scope-404, not-configured-404, full-panel fan-out, dimension subset + dedupe, fail-soft per judge). **No CHANGELOG entry** (app surface). Tracker: `planning/features/f5.1.md`. Docs: new `design-evaluation.md`.
- **2026-06-05 — F4.6 session state machine ([PR #36](https://github.com/human-centric-engineering/conquest/pull/36)).** Closes P4 — gives `AppQuestionnaireSession` a lifecycle (`active | paused | completed | abandoned`) with a deterministic transition machine, an append-only audit trail (`AppQuestionnaireSessionEvent`, one row per transition), resume logic, and a cost-cap event hook wired for F6.3/F6.5 to fire. **Decisions (confirmed):** **deterministic, no LLM** (no capability/agent/sub-flag/prompt/schema — a pure transition table + the seam writes, gated by the master flag only); **vocab keeps `active`, adds `paused`** (Option B — the code shipped `active` in F4.4, so an additive `SESSION_STATUSES` edit over a rename, no data-backfill migration); **one thin admin transition route** (`pause | resume | abandon`) — completion stays the single submit entrypoint on F4.5's `/complete` route, an events-read route deferred to P8; **the route excludes the preview session** (`isPreview: false`) so it can't brick a version's `/complete` submit — the machine is for real respondent sessions (F6.1) and 404s every session that exists today, exercised by hand via the seam; **status + event written atomically** in one `$transaction`; **`markSessionCompleted` generalised** into `transitionSession` (moved to `_lib/sessions.ts`, re-exported from `answer-slots.ts` so the F4.5 route is untouched — completion now writes a `completed` event); **`cost_cap_reached` wired but not fired** (`recordCostCapReached` ready for F6.3/F6.5), **`created` reserved** for real sessions (F6.1); **`AppQuestionnaireSessionEvent` fully Prisma-modelled** (no raw-SQL object → no drift probe), migration `20260605141717_app_session_event` create-only + stripped; **minimal resume state** (`{ status, answeredSlots }`). **Tests:** unit (the full 4×4 transition matrix — apply/noop/illegal, `isTerminal`, `assertTransition`, `eventTypeFor`, vocab parity) + schema-shape (the model DMMF + migration SQL); integration (seam — atomic apply, noop no-write, illegal pre-write throw, the wrappers, `recordCostCapReached`, `loadSessionResumeState`; route — gate order, invalid action 400, scope 404, dispatch + resume payload, illegal → 409). **No CHANGELOG entry** (app surface). Tracker: `planning/features/f4.6.md`. Docs: `session-state-machine.md`.
- **2026-06-05 — F4.5 completion logic ([PR #35](https://github.com/human-centric-engineering/conquest/pull/35)).** The close primitive of P4 — decides when the agent may **offer to submit** (against the version's completion config + a required-questions gate), phrases that offer, resolves accept/hold, runs the F4.3 contradiction **completion-sweep** at offer time, and transitions the session `active → completed` on a clean submit; the first P4 feature to move session lifecycle state. **Decisions (confirmed):** **two layers — deterministic gate + LLM phrasing** (eligibility `assessCompletion` is pure math — whether to offer; the capability only phrases the offer — how to say it; the LLM never decides eligibility, so the required-questions gate stays authoritative); **required gate blocks the offer** (an unanswered required slot → `blocked_on_required` even when weighted coverage is met); **sweep conflict → hold for review** (on accept, sweep findings do NOT auto-submit — return findings, leave `active`, reconcile via F4.4, re-offer); **two routes** (`completion-status` read-only assess + optional composed offer, no persistence; `complete` accept/hold action — persists status, runs the sweep); **no new config / no `sweep_only`** (maps onto the committed flat fields + the existing `shouldRunDetection(...,'completion-sweep')`); **reuse not reinvention** (`assessCompletion` reuses the F4.1 coverage helpers via a backward-compatible `CoverageContext` narrowing; routes reuse `buildSelectionContext` + F4.3's capability for the sweep); **no new model** (builds on F4.4's session/answer tables; adds `markSessionCompleted`); **completion sub-flag does NOT 404 its route** (a disabled `APP_QUESTIONNAIRES_COMPLETION_ENABLED` still returns the free deterministic assessment — only the paid LLM phrasing is gated); fail-soft everywhere. **One hardening migration** `20260605141500` — a raw-SQL partial unique index (`idx_app_questionnaire_session_preview_per_version`, WHERE `isPreview`) making the preview-session get-or-create race-safe; drift-probed. Sub-flag seed `017`, agent/capability seeds `015`/`016`; `completionLimiter` (60/min). **Tests:** unit (29 — `assessCompletion` incl. required-blocks-despite-coverage + epsilon boundary, `resolveCompletion`, schema/prompt/vocab) + capability integration (14) + seam (+2) + routes (`completion-status` 12, `complete` 16 — accept→submit, accept→hold_for_review keeps active, sub-flag-off skips sweep). **No CHANGELOG entry** (app surface). Tracker: `planning/features/f4.5.md`. Docs: `completion-logic.md`.
- **2026-06-05 — F4.4 answer refinement ([PR #34](https://github.com/human-centric-engineering/conquest/pull/34)).** The answer-update primitive of P4 — given a respondent's captured answers + new context (a clarifying message and/or a flagged contradiction), decide per slot to **refine** (value evolved → provenance `refined`), **overwrite** (mistaken capture → provenance preserved), or **leave**, preserve a `refinementHistory`, and **write** the change; the consumer of F4.3's `suggestedProbe` handoff. **Decisions (confirmed):** **persistence is in scope — a deliberate divergence from F4.1–F4.3** (those were no-persistence previews; F4.4 introduces the **persistence foundation** pulled forward from F4.6 — a minimal `AppQuestionnaireSession` + the `AppAnswerSlot` model with `refinementHistory` + a migration + a real write path, which F4.6/F6.1 build their live loop on); **`overwrite` keeps the original provenance, only `refine` → `refined`** (refined = the value evolved, a precise audit signal; a typo fix appends a `correction` history entry but keeps the label); **own agent + capability** (the LLM decides refine/overwrite/leave; the `applyRefinement` apply step is pure deterministic logic); **no clock in the pure core** (the seam stamps `createdAt` at write time); **reuse F4.2 value validation** (`validateAnswerValue` — a refined value obeys the same per-type rules); **preview session for the write path** (`isPreview`, reused idempotently, excluded from P8 analytics — no real respondent sessions yet); own sub-flag `APP_QUESTIONNAIRES_ANSWER_REFINEMENT_ENABLED` (off); fail-soft route. Core-local `REFINEMENT_ACTIONS`/`REFINEMENT_SOURCES`; seeds `012`/`013`/`014`; `answerRefinementLimiter` (60/min). **Tests:** unit (40 — schema, normaliser drops, `applyRefinement` refine→refined+history / overwrite-keeps-provenance / no-mutation, summary) + persistence-seam integration (11 — idempotent preview reuse, upsert composite key, `persistRefinement` stamp) + capability integration (21) + route integration (18 — gate order, persist-then-refine wiring, fail-soft). **No CHANGELOG entry** (app surface). Tracker: `planning/features/f4.4.md`. Docs: `answer-refinement.md`. (See [[f44-introduced-answer-persistence]].)
- **2026-06-05 — F4.3 contradiction detection ([PR #33](https://github.com/human-centric-engineering/conquest/pull/33)).** The consistency-check primitive of P4 — given a respondent's captured answers across a version's slots, detect genuine cross-slot logical contradictions and **surface them for confirmation, never auto-overwrite** (catch "no children" then "my daughter's at college"). Detection only; resolution is F4.4. Built as pure core + capability + no-persistence preview route, mirroring F4.1/F4.2. **Decisions (confirmed):** **align to the committed schema vocab, not the plan prose** (F3.1 locked `contradictionMode = off | flag | probe` + `contradictionWindowN`; keep that **behaviour** axis — `flag` surfaces, `probe` follows up — and model **cadence** as pure logic, no migration; the plan prose updated to match); **cadence is a pure scheduler now** (`shouldRunDetection(mode, windowN, phase)` covers `turn` + `completion-sweep` from day one; `every_n_turns` deferred to F4.6's real loop); **surface never overwrite** (returns `ContradictionFinding[]` — which slots conflict, why, severity, confidence, and under `probe` a `suggestedProbe` handoff — writes nothing); **detector-local `CONTRADICTION_SEVERITIES`**; **resilient schema, normaliser enforces semantics** (drops one odd finding rather than failing the pass — the F4.2 doctrine; symmetric-pair dedupe keeps higher confidence); **≥2 answers required** (meaningless below two — enforced at schema, context builder → 400, and normaliser); new contradiction-detector agent; own sub-flag `APP_QUESTIONNAIRES_CONTRADICTION_DETECTION_ENABLED` (off); fail-soft route. Seeds `009`/`010`/`011`; `contradictionDetectionLimiter` (60/min). **Tests:** unit (36 — schema, normaliser drop/dedupe/clamp/flag-strips-probe, scheduler off/sweep/turn-window, prompt, vocab parity) + capability integration (16) + route integration (17 — gate order, insufficient-answers 400, config-defaulted + body-overridden mode/window, fail-soft). **No CHANGELOG entry** (app surface). Tracker: `planning/features/f4.3.md`. Docs: new `contradiction-detection.md`.
- **2026-06-05 — F4.2 answer extraction into slots ([PR #32](https://github.com/human-centric-engineering/conquest/pull/32)).** The answer-capture complement of F4.1's "which question next?" — given a respondent's message + the active question + session context, produce `(value, confidence, provenance, rationale, label)` for one or more slots, including **side-effects** (one message answering several questions, each recorded as a separate intent with `isActiveQuestion` set by comparing `slotKey` to the active key). Built as **pure core → capability → no-persistence preview route** across 3 risk-sequenced PRs on one branch, mirroring F4.1/F1.1; persists nothing (the `AppQuestionnaireSession`/`AppAnswerSlot` tables are F4.6's), emitting a version-agnostic `AnswerSlotIntent[]` (the F1.1 `ChangeRecordIntent` analogue) for F4.6 to persist. **Pure core** (PR1, `lib/app/questionnaire/extraction/`, Prisma-free): `types.ts` (`ExtractionContext`, `ExtractionSlotView`, `AnswerSlotIntent`, `AnswerExtractionResult`), `extraction-schema.ts` (Zod LLM contract with `value: z.unknown()` + `z.toJSONSchema` + `validateAnswerExtraction`), `answer-value.ts` (per-type value validation reusing F2.1 `typeConfigSchemaFor` for choice-membership / likert + numeric bounds — same open-value split as ingestion's `suggestedTypeConfig`), `extraction-prompt.ts` (provider-agnostic prompt + retry), `answer-intents.ts` (`normalizeAnswerIntents` — unknown-slot drop, value-fails-type drop, `direct`-without-quote downgrade, dedupe, active-vs-side-effect tagging). **Provenance vocabulary** (in `../types.ts`): `ANSWER_PROVENANCES = [direct, inferred, synthesised, refined]` as single-source tuple; `EXTRACTOR_EMITTED_PROVENANCES` (a `satisfies` subset) drops `refined`, reserved for F4.4's refinement flow so F4.4 starts emitting it with no tuple edit; the Zod enum derives from the subset. **Capability** (PR2): `AppExtractAnswerSlotsCapability` (`lib/app/questionnaire/capabilities/extract-answer-slots.ts`) — provider-agnostic `runStructuredCompletion` → parse/retry → cost-sum, then value-validate + normalise into `AnswerSlotIntent[]`; resolves the `chat` tier (per-turn, snappy — not the heavier `reasoning` ingestion uses); `processesPii=true` with a counts-only `redactProvenance`. New **answer-extractor agent** distinct from the document extractor (002) and selection agent (005) — per-turn, higher volume, own monthly budget; seeds `006-answer-extractor-agent` + `007-answer-extraction-capability`, registered in `lib/app/capabilities.ts` + the barrel. **Sub-flag + preview route** (PR3): `APP_QUESTIONNAIRES_ANSWER_EXTRACTION_ENABLED` (off by default) + `isAnswerExtractionEnabled()`, seed `008-answer-extraction-flag` (disabled); `answerExtractionLimiter` (60/min) in `_lib/rate-limit.ts`; `_lib/extraction-context.ts` (the feature's only Prisma — loads slots, resolves active key, builds candidate pool = unanswered + active, hard `.max()` capped); `POST …/versions/:vid/extract-answer` ordered flag → admin auth → sub-flag → validate → sub-cap → context → dispatch → **fail-soft** (capability error → 200 with empty intents + a `diagnostic`, never 5xx, so the engine keeps the conversation going). **Decisions:** unknown `activeQuestionKey` → **400** (key is load-bearing), a deliberate divergence from next-question's drop-unknown-keys leniency; off until **both** `APP_QUESTIONNAIRES_ENABLED` + the sub-flag are on (extraction spends an LLM call every turn), 404 when the sub-flag is off. **Tests — 80 cases:** 50 unit (`tests/unit/lib/app/questionnaire/extraction/` — schema round-trip + named-path issues, `answer-value` per type incl. out-of-range/wrong-shape, prompt-builder structure lists types + the 3 emittable labels, normaliser drop/downgrade/dedupe/active-vs-side-effect, provenance-vocabulary parity); 15 integration-capability (real dispatcher + `runStructuredCompletion` with mocked provider — happy path active + side-effect, value-fails-type drop, empty, retry, no-silent-failure, provider fail-closed, cost-log isolation, redaction); 15 integration-route (gate order, auth 401/403, scope-404, unknown-active-key 400, body validation, 429, dispatch wiring, answered-slot exclusion, fail-soft, missing-agent 404). `npm run validate` + full questionnaire suite green. **Docs:** new `.context/app/questionnaire/answer-extraction.md` (two-shape contract, candidacy, provenance vocabulary + the `refined`/F4.4 seam, normaliser, capability + agent + sub-flag, preview route, no-persistence boundary) + cross-refs from `selection-strategies.md` (the `confidence` consumer) and `configuration.md` + README index. **No CHANGELOG entry** (app surface, not the Sunrise platform public surface; consistent with F1.1–F4.1). Tracker: `planning/features/f4.2.md`. Gated each PR with `/pre-pr`, `/code-review`, `/test-review`; `/security-review` on PR2 (LLM I/O + PII redaction) and PR3 (new authed input surface + LLM cost + respondent PII through the route).
- **2026-06-04 — F4.1 selection strategies ([PR #30](https://github.com/human-centric-engineering/conquest/pull/30)).** Opens P4 (conversational engine, non-streaming): the per-turn "which question next?" intelligence, built as pure functions exercisable by Vitest before any streaming surface exists (P6 wraps streaming around it later). Consumes the `selectionStrategy` config seam F3.1 shipped as a no-op. Delivered across **3 risk-sequenced PRs on one branch**. **Pure core** (`lib/app/questionnaire/selection/`, Prisma-free): a grader-style `registry.ts` (slug → `SelectionStrategyPlugin` + reset + `KNOWN_STRATEGY_SLUGS`), `types.ts` (`SelectionContext`, `SelectionDecision`, `StrategyDeps`, weighted constants), `context.ts` helpers (`unanswered`, `coverageRatio`, `requiredFirstPool`, `terminalDecision`), and `strategies/{sequential,random,weighted,adaptive}.ts` auto-registered via the `index.ts` barrel — strategies run over an in-memory `SelectionContext` assembled by the caller (test harness now, engine later), so they never touch Prisma since session/turn/answer tables don't exist yet (F4.6/P6). `sequential` keeps strict document order; `random` is uniform among unanswered, seeded on `session+round` for crash-replay idempotency (no `Math.random`); `weighted` scores weight × section-coverage × low-confidence pull. `random`/`weighted` enforce **required-before-optional precedence**. `random` added to the `SELECTION_STRATEGIES` enum in `../types.ts` (was `[sequential, weighted, adaptive]`, now 4) plus an editor label. **Weighted tuning constants live in code, not config** (`UNDERCOVERED_SECTION_BONUS`, `LOW_CONFIDENCE_MULT`, `LOW_CONFIDENCE_THRESHOLD`), tuned like the cost-estimation token constants; per-questionnaire control is a future PR. **Adaptive** is the full LLM+pgvector flavour: embeds the last message → pgvector top-K over unanswered candidates → seeded `app-questionnaire-selector` agent picks the follow-up via `drainStreamChat`; **fail-soft to `weighted`** on every failure path (no deps, no history, no embeddings, LLM/budget error, off-pool/unparseable pick) — never throws. Its impure surface is **injected via `StrategyDeps`** (`embedText`, `rankByVector`, `llmPick`) so `lib/` stays Prisma-free and mockable; real deps are wired at the route seam (`_lib/adaptive-deps.ts` `buildAdaptiveDeps`, `_lib/slot-embeddings.ts` for `embedBatch` backfill + pgvector `<=>` rank). Adaptive is gated behind its own sub-flag `APP_QUESTIONNAIRES_ADAPTIVE_STRATEGY_ENABLED` (**off by default** — it spends per turn) on top of the master app flag; off → hidden from the config picker (threaded page → version-editor → config-editor) and degraded to `weighted` at run time. **Schema** (migration `20260604181453_app_question_slot_embedding`): `AppQuestionSlot.embedding vector(1536)` as Prisma `Unsupported(...)` with raw-SQL read/write + an HNSW index added by raw SQL — generated `--create-only` with the generated diff's phantom knowledge-index `DROP`s stripped (the documented `prisma migrate dev` footgun); knowledge indexes verified intact via drift check. **APIs** (`withAdminAuth`, Zod): `POST …/versions/:vid/next-question` — preview route, **no persistence**, runs the configured/overridden strategy against a caller-supplied answer state (keyed on slot `key` via `_lib/selection-context.ts`) → `{ strategy, decision, question? }`; `POST …/versions/:vid/embed-questions` — slot-embedding backfill. Per-flow rate-limit sub-caps added (`adaptiveSelectionLimiter`, `embedSlotsLimiter`). **Plumbing:** a registered drift probe for the new HNSW index; two seed units (`004-adaptive-selection-flag`, `005-selection-agent`). **Decisions:** pure strategies over an in-memory context (no Prisma until F4.6/P6); impure deps injected not imported; fail-soft adaptive so a respondent never sees a broken turn; embedding column mirrors the platform width (`AiKnowledgeChunk`). **Operator note:** adaptive needs slot embeddings — run `embed-questions` for a version, then flip the sub-flag; without embeddings or with the flag off it transparently runs as `weighted`; on-write embedding generation is deferred (backfill-only today). **Tests:** unit suites for registry parity, `context` helpers, every `sequential`/`random`/`weighted`/`adaptive` edge case (no-remaining, cap/threshold completion, random determinism + idempotency, weighted scoring math, every adaptive fallback), `slot-embeddings` + `adaptive-deps` seams, and integration coverage of both routes (gate order, validation, per-strategy wiring, adaptive flag on/off, scope-404, counts, force). `npm run validate` + full questionnaire suite green. Docs: new `.context/app/questionnaire/selection-strategies.md` (the four algorithms, terminal conditions, the DI seam, the adaptive flow + flag, the embedding column) + a cross-ref from `configuration.md`. **No CHANGELOG entry** (app surface, not Sunrise platform). Tracker: `planning/features/f4.1.md`. **Gates all green:** `/pre-pr` (clean), `/code-review high` (8 findings fixed, incl. a float-epsilon completion bug and an embedding-dimension footgun), `/test-review` (8 findings fixed — 429-path coverage + limiter-mock isolation), `/security-review` (clean — raw SQL parameterized, auth/flag-gated, IDOR-scoped; run on PR2 + PR3).
- **2026-06-04 — F3.4 demo-client invitation branding (DEMO-ONLY) ([PR #29](https://github.com/human-centric-engineering/conquest/pull/29)).** Themes the respondent invitation email per attributed demo client (the prospect's first branded touch), closing out the decomposed P2.5 invitation-branding slice (deps F2.5.1 + F3.2). **Open decision resolved → themed invites adopted:** because the invite is branded, the theming module is built now in **P3** rather than deferred to P7; F7.1's chat surface will consume the already-built module via `themeToCssVariables()`. Everything is `// DEMO-ONLY:` and strips in one grep sweep. **Schema + migration** (`20260604144743_app_questionnaire_invitation_branding`): `AppDemoClient` gains 4 nullable theme columns (`ctaColor`, `accentColor`, `logoUrl`, `welcomeCopy`) where null on any field = the Sunrise default; `AppQuestionnaireInvitation` gains `demoClientId` as a brand **snapshot** (real relation, `onDelete: SetNull`, indexed) pointing at the client directly so later re-attribution doesn't change an already-sent invitation's brand (O(1) themed lookup). Generated `--create-only` with schema-fold pgvector DDL hand-stripped; `db:drift-check` 9/9, platform indexes intact. **Theming module** (`lib/app/questionnaire/theming/`, Prisma-free): `resolveTheme(client | null)` fills nulls with `SUNRISE_THEME_DEFAULTS`; `themeToCssVariables(theme)` for the F7.1 UI emits the logo as a quoted, CSS-escaped `url("…")` so the value can't break out of `url()` context; `themeFields` (Zod) validate hex colours + an absolute **https** logo URL and spread into the demo-client create/update schemas. **Wiring:** invitation create snapshots `demoClientId` onto each row and resolves the brand once per batch (generic/unattributed → all-Sunrise theme, byte-identical to the pre-F3.4 email); resend themes from the invitation's own snapshot, with a deleted client (stale FK) degrading to defaults, not an error; `emails/questionnaire-invitation.tsx` renders `ctaColor` (button), `accentColor` (fallback link), `logoUrl` (header image) and `welcomeCopy` (intro line), with `theme` defaulting to `resolveTheme(null)` for the email-preview tooling; admin demo-client form gains an "Invitation branding" fieldset (4 fields, `<FieldHelp>` on each, blank = Sunrise default). **Tests:** theming unit tests (incl. the CSS-escape hardening), themed email render tests, extended demo-client + invitation route integration coverage (denorm, themed resend, stale-FK fallback, flag gate) and a schema-shape guard for the new migration; `npm run validate` green, 378 affected tests pass, drift-check 9/9. Docs: updated `.context/app/questionnaire/{demo-clients,invitations,schema}.md`. **No CHANGELOG entry** (app surface — `app-questionnaire.prisma` + `lib/app/questionnaire/**` — not the Sunrise platform; consistent with the F2.5.1 precedent). Tracker: no dedicated `f3.4.md` — distributed/relocated sub-feature documented in the development plan and the F3.2 invitation-branding area. **Gates all green:** `/pre-pr` (green; one coverage regression fixed), `/security-review` (clean, 0 findings), `/code-review high` (3 findings, all fixed — hardened `themeToCssVariables` against CSS injection, corrected an `accentColor` doc inaccuracy, de-duplicated the https-URL validator), `/test-review` (8 findings ≥80, all fixed — always-green async test, missing error-envelope assertions, stale-FK + flag-off coverage gaps).
- **2026-06-04 — F3.3 pre-launch cost estimation ([PR #28](https://github.com/human-centric-engineering/conquest/pull/28)).** Third feature of P3 — gives an admin a heuristic projection of LLM spend for running respondents through a questionnaire version (per-session and per-questionnaire) before launch, so they can size the F3.1 per-session `costBudgetUsd` cap; read-only, API-first, gated by `APP_QUESTIONNAIRES_ENABLED` (404 when off). **Pure core** (`lib/app/questionnaire/cost-estimation/` — barrel `index.ts`, `types.ts`, `estimate.ts`; Prisma-free, re-exported from `lib/app/questionnaire/index.ts`): `effectiveQuestionsPerSession(...)` + `estimateSessionCost(input)` over numeric inputs only. A session is modelled as one turn per asked question; the dominant driver is conversation history replayed each turn, so input tokens grow **quadratically** in `Q` (`HISTORY × Q(Q−1)/2`) — a flat per-question model would under-count long questionnaires. Conservative, tunable constants (`SYSTEM_PROMPT_TOKENS=1500`, `HISTORY_TOKENS_PER_PRIOR_TURN=250`, `OUTPUT_TOKENS_PER_TURN=400`, range `×0.5/×1.7`). `Q` honours the F3.1 caps: `min(questionCount, maxQuestionsPerSession ?? questionCount)`, floored by `min(minQuestionsAnswered, questionCount)`. **Decisions:** **heuristic-only — no empirical mode** (the session/turn engine is P4/P6, so zero real runs to calibrate against; `basedOn` always `'heuristic'`, wide range, copy says so — empirical mode keyed on `AppQuestionnaireTurn` actuals is a future PR, the pattern Sunrise's `workflow-cost.ts` already uses); **provider-agnostic pricing, unknown-price ≠ free** — model resolved via `getDefaultModelForTaskOrNull('chat')`, priced via `getModel`; a model with no registry price (or a `0` rate) returns `pricingKnown: false` with USD withheld and a note, never a misleading `$0.00` (token volume still estimated). **API:** `GET …/versions/:vid/cost-estimate?respondents=N` (`cost-estimate/route.ts`, GET-only, registered as `versionCostEstimate(id, vid)` in `lib/api/endpoints.ts`): flag-gate → `withAdminAuth` → `loadScopedVersion` scope-404 → Zod-validate `respondents` → `Promise.all`(slot prompts, config caps, default chat model) → `getModel` pricing + `Σ estimateTokens(slot.prompt)` → `estimateSessionCost` → `successResponse`. Pure math, no LLM call, **no audit, no rate-limit sub-cap** (inherits section 100/min). **UI:** shared `components/admin/questionnaires/cost-estimate-card.tsx` (`variant 'card' | 'banner'`) — full card in `config-editor.tsx` under Budget & caps (expected-respondents input + over-budget note vs live `costBudgetUsd`) and a compact banner on the invitations page above the invite form (against the newest launched version). One GET per surface at `respondents=1`; `perQuestionnaire = perSession × respondents` scaled client-side, so changing the respondent count never refetches. **Tests:** unit (cap/floor/bounds, ordered low<mid<high, linear respondent scaling, quadratic input-token growth vs question count, cap applied to asked count, pricing-unknown for null **and** `0` rate, zero-question all-zero + note, exact token formula); integration (gate order, 401/403, scope-404, `respondents` validation, happy path asserting `getModel` + per-slot `estimateTokens` consulted + `perQuestionnaire = perSession × respondents`, default-model + no-saved-config fallback, pricing-unknown branches). `npm run validate` + full questionnaire suite (485) green. Docs: new `.context/app/questionnaire/cost-estimation.md` (heuristic, every constant, no-empirical-mode note, unknown-price contract, endpoint, both surfaces) + cross-ref from `configuration.md`'s cost-budget setting. **No CHANGELOG entry** (app surface, not Sunrise platform). Tracker: `planning/features/f3.3.md`. **Gates all green:** `/pre-pr` (clean), `/code-review high` (5 fixes: newest-launched-version banner, non-finite-respondents guard, respondent cap, `scaleRange` reuse, question-count reload key), `/test-review` (5 findings ≥80 applied), `/security-review` (clean — read-only, no mutation, no new external surface).
- **2026-06-04 — F3.2 invitation flow ([PR #27](https://github.com/human-centric-engineering/conquest/pull/27)).** Second P3 feature: the end-to-end questionnaire invitation lifecycle — admin invites respondents (single or bulk) to a **launched** questionnaire, the system emails a tokenised link, the respondent opens it and **registers a real better-auth account**; flag-gated `APP_QUESTIONNAIRES_ENABLED` (404 when off, ships dark). Built as **2 PRs bundled into the feature** (admin side · respondent registration), one `/security-review` each. **Schema** (migration `20260604103311_app_questionnaire_invitation`): `AppQuestionnaireInvitation` — **version-scoped, deliberately NOT Sunrise's email-keyed `Verification` store** (the invitation is a per-version, lifecycle-bearing domain object queried through the version relation, no `questionnaireId` denormalisation); FK→version `onDelete: Cascade`, `tokenHash @unique` (SHA-256 at rest, plaintext only in the email URL), `status @default("pending")`, lifecycle timestamps (`sentAt openedAt registeredAt revokedAt`), `expiresAt`, indexes on `versionId/status/email/userId`; reverse `invitations` on the version. Migration hand-stripped of phantom pgvector/`searchVector` DDL (schema-guard asserts it stays out). **Pure core** (`lib/app/questionnaire/invitations/**`, Prisma-free): `token.ts` (`mintInvitationToken`/`hashInvitationToken`, `crypto` only), `status.ts` (transition table + `assertInvitationTransition`), `schemas.ts` (single-email or `emails[]`, in-batch dedup, max batch, email format), `types.ts` (`APP_INVITATION_STATUSES`, `INVITATION_TOKEN_EXPIRY_DAYS`), `views.ts` (`InvitationView`, never exposes `tokenHash`). **Lifecycle state machine:** `pending → sent → opened → registered → started → completed | revoked`; F3.2 reaches `registered`; **`started`/`completed` are seam states** (enum values only, no transition logic until P6/P7 sessions). **APIs — admin** (`app/api/v1/app/questionnaires/[id]/invitations/`, `withAdminAuth`, flag-gated, audited): `GET` enriched list (status filter, pagination), `POST` send (resolve currently-launched version, `409 INVITE_NO_LAUNCHED_VERSION` if none → per-email app-layer dedup of non-revoked/non-terminal `(versionId,email)`, mint token, create row, `sendEmail`, per-recipient result, `inviteLimiter`, audit `questionnaire_invitation.create`), `[invitationId]` `PATCH` revoke, `[invitationId]/resend` `POST`. **APIs — public respondent** (`app/api/v1/app/invitations/`, token-gated, no admin auth): `metadata` `GET ?token=` (hash→lookup, expired→410, invalid→404, hit → `{ questionnaireTitle, inviteeName, status }` + `sent→opened`), `accept` `POST { token, password, name? }` (lookup → **email derived server-side from the row** → `signUpEmail` → set `emailVerified` → set `userId`/`registered` → `signInEmail` forwarding Set-Cookie for auto-login; email-taken → typed `409 ACCOUNT_EXISTS`, claim-via-login deferred to P7; `acceptInviteLimiter`). **Launch-blocker wiring** (`_lib/launch-blockers.ts`): first non-zero `countLaunchBlockers(versionId)` → `{ invitations: <non-revoked, non-terminal count>, sessions: 0 }`; a live invitation **pins** its launched version (edit → fork a draft; un-launch/archive → 409); the two callers switch to the route-local Prisma counter while the pure `authoring/launch-blockers.ts` keeps the `LaunchBlockers` type + `hasLaunchBlockers`. **Email:** app-owned `emails/questionnaire-invitation.tsx` (F3.4 themes it). **UI:** admin sub-route `app/admin/questionnaires/[id]/invitations/page.tsx` (`invitations-table.tsx`, `invite-form.tsx` single+bulk paste, `invitation-status-badge.tsx`, `<FieldHelp>`, linked from the detail page); respondent landing + set-password page at `app/(auth)/questionnaire-invite/page.tsx`. **Decisions:** dedicated version-scoped model not `Verification` (reusing Sunrise _primitives_ — token recipe, `sendEmail`, `inviteLimiter`/`acceptInviteLimiter`, accept-invite signup machinery); identity = full better-auth account; **first app→`User` references** — `userId` (respondent, nullable) + `invitedByUserId` (admin) are plain `String`, no `@relation` (UG-1), erasure stance recorded in `invitations.md`; app-layer dedup not a DB `@@unique` (revoke → re-invite must work). **Tests:** schema guard (dmmf + migration-SQL, no-platform-DDL), unit (token stable & `token !== hash`, status transitions incl. `started/completed` unreachable, schemas single/bulk/dedup/max-batch, `InvitationView` excludes `tokenHash`), integration admin (gate order, send + bulk per-recipient, `409` no-launched-version, dedup skip, rate limit, audit, GET filter/pagination, revoke, resend, launch-blocker refuses un-launch + forks-on-edit), integration public (metadata `opened`/410/404, accept creates user + sets `userId`/`registered` + forwards cookies + `409` email-taken). `npm run validate` + full suite (20,453) green. Docs: new `.context/app/questionnaire/invitations.md` + admin-ui cross-ref. **No CHANGELOG entry** (app surface, not Sunrise platform). Tracker: `planning/features/f3.2.md`. **Gates all green:** `/pre-pr` (clean), `/security-review` (no findings), `/code-review high` (fixed: resend no longer invalidates a working link on email failure, admin list no longer silently truncates at 20, resend uses the pinned-version title, bulk send degrades gracefully on transport throw), `/test-review` (assertions hardened; 429/audit/branch tests added).
- **2026-06-04 — F3.1 questionnaire configuration + launch gate ([PR #26](https://github.com/human-centric-engineering/conquest/pull/26)).** First feature of P3 — adds the per-version run-time configuration an admin authors before launch, plus a tightened launch gate that makes a version genuinely launchable. Gated by `APP_QUESTIONNAIRES_ENABLED`, API-first, riding the F2.1 scoped-version + fork-on-launched seams. **Schema** (migration `20260604090526_app_questionnaire_config`): `AppQuestionnaireConfig` 1:1 with `AppQuestionnaireVersion` (`versionId @unique`, `onDelete: Cascade`) so it forks with the version, reverse `config AppQuestionnaireConfig?`; typed scalar columns `selectionStrategy`, `minQuestionsAnswered`, `coverageThreshold`, `costBudgetUsd?`, `maxQuestionsPerSession?`, `voiceEnabled`, `contradictionMode`, `contradictionWindowN`, `anonymousMode` + `profileFields Json @default("[]")`; usual schema-fold strip + schema-guard. **Lazy materialization:** no row at ingest/version-create — reads resolve an absent row to `DEFAULT_QUESTIONNAIRE_CONFIG`, the first PATCH upserts it, the fork copies only when present, leaving the F1.1 ingest and no-config fork paths byte-for-byte unchanged. **Pure core** (`lib/app/questionnaire/**`, Prisma-free): `types.ts` adds `SELECTION_STRATEGIES`, `CONTRADICTION_MODES`, `PROFILE_FIELD_TYPES` const tuples, `ProfileFieldConfig`, `QuestionnaireConfigShape`, `DEFAULT_QUESTIONNAIRE_CONFIG`; `authoring/config-schema.ts` adds `profileFieldSchema` (field-type enum `text | email | number | select`; `select` requires a non-empty distinct `options` list) and `updateConfigSchema` (all-optional partial, at-least-one refine, contradiction mode/N coherence + unique profile-field keys via `superRefine`). **Read-view** (`views.ts`/`_lib/detail.ts`): `ConfigView` (`+ saved: boolean`) on `VersionGraphView`; `toConfigView(row | null)` returns defaults with `saved:false` when absent, selected into `getVersionGraph` so config rides the existing single fetch — no separate GET. **API** (`config/route.ts`): `PATCH …/versions/:vid/config`, ordered flag-gate → `withAdminAuth` → `loadScopedVersion` (scope-404) → `validateRequestBody(updateConfigSchema)` → `forkVersionIfLaunched` preamble → read `before` → upsert (create-with-defaults / update) → `logAdminAction('questionnaire_config.update')` + `computeChanges` → `successResponse(toConfigView(row), forkMeta(fork))`; registered as `versionConfig(id, vid)`. **Fork** (`_lib/fork.ts`): copies the config row into the new draft when present (`CONFIG_SELECT` so future columns ride along). **Launch gate** (`status/route.ts`): `assertLaunchable` extends F2.1's goal + ≥1 section + ≥1 question with `audience` (`hasAudience` helper; empty `{}` counts as not-populated) and a **saved config row** (`appQuestionnaireConfig.count`), adding `audience`/`config` to the `missing` map — "config populated" deliberately means a row exists (the admin opened and saved the editor at least once). **UI:** `components/admin/questionnaires/config-editor.tsx` — controlled state + the shared `run` runner, `Card`/section layout, `<FieldHelp>` ⓘ on every non-obvious field, `Switch`/`Select` primitives, dynamic add/remove profile-fields list with a conditional options input for `select`; rendered as a collapsible **Configuration** section inside `version-editor.tsx` (no new page/route/SSR fetch). **Decisions:** dedicated lazy 1:1 model; profile fields as JSON `ProfileFieldConfig[]` (read wholesale at session start); launch rule keys on a saved row rather than a semantic predicate; inline editor over a sub-route; fork-on-launched compatible. **Tests:** schema-guard (DMMF block incl. `profileFields → Json`, the relation + reverse, migration-SQL `CREATE TABLE` + `versionId` FK `ON DELETE CASCADE` + unique index + no-platform-DDL guard); config-schema unit (24 cases — full/partial PATCH, contradiction coherence, profile-field unique-key / select-requires-options / non-select-forbids-options, bad enum, coverage bounds); config-route integration (gate order, 401/403, scope-404, fork preamble + `meta.forked`, upsert create-vs-update, audit); fork config-copy; extended launch-gate cases (missing audience/config → 400 with right `missing` keys, all-present → launch). `npm run validate` + full suite (20,380) green. Docs: new `.context/app/questionnaire/configuration.md` + cross-refs from `README.md`/`schema.md`. No CHANGELOG entry (app surface, not Sunrise platform). Tracker: `planning/features/f3.1.md`. **Gates all green:** `/pre-pr`, `/security-review` (clean — a new authenticated mutation surface), `/code-review` (4 findings fixed — nullable-cap coercion, blank-coverage guard, decimal flooring, fork column-relisting hazard), `/test-review` (3 findings fixed).
- **2026-06-04 — F2.4 re-ingest ([PR #25](https://github.com/human-centric-engineering/conquest/pull/25)).** The **final P2 feature**, closing the loop ingest → review → edit → tag → version → **re-ingest**: an admin uploads a _replacement_ source doc against an existing **draft** version, the extractor re-runs, and that draft's structure + editorial change log are **replaced in place**. Behind `APP_QUESTIONNAIRES_ENABLED` (404 when off), API-first. **No migration** — every column needed (incl. the reserved nullable `bytes`) already exists from F1.1; F2.4 still doesn't persist `bytes` (no diff-against-stored-source consumer; stays deferred). **Refactor (behaviour-preserving):** factored F1.1's shared "bytes → validated extraction" stretch into `_lib/extract-pipeline.ts` (`parseAndGuardUpload` + `dispatchExtraction`/`extractFromDocument`) and the inner graph write `writeGraph`/`writeSourceDocument` into `_lib/persist.ts`, both **single-sourced by F1.1's `handleIngest` and re-ingest** — F1.1's 22 existing ingest route tests are the regression net. **Writer** (`_lib/reingest.ts`): `reingestVersion({versionId, extraction, admin, source})` in one `executeTransaction` — `deleteMany` the version's change records, tag vocabulary, and sections (cascades slots → slot-tag assignments), `update` `goal`/`audience`/provenance from a fresh `mergeGoalAudience`, `writeGraph(...)`, then `create` the appended `AppQuestionnaireSourceDocument` (prior docs kept as provenance, newest active); `versionNumber`/`status`/id untouched; re-asserts `status === 'draft'` **inside the transaction** (closes a TOCTOU against a concurrent launch). **Route** (`POST …/versions/:vid/reingest`): flag-gate → `withAdminAuth` → `ingestLimiter.check(adminId)` (reuses F1.1's per-admin 10/min sub-cap) → `loadScopedVersion` (404 unknown/cross-id) → **draft-only else typed `409 REINGEST_NOT_DRAFT`** → `parseAndGuardUpload` → **version-scoped dedup short-circuit** → `dispatchExtraction` → `reingestVersion` → `logAdminAction('questionnaire.reingest')`; registered as `versionReingest(id, vid)`. **Decisions:** **replace-in-place, not a new version** (destructive of manual edits + tag assignments, guarded by UI confirm); **draft-only** — the one place F2.4 diverges from the F2.1–F2.3 fork-on-launched pattern; **version-scoped dedup** — identical bytes already on the target version's _active_ source short-circuit to a `200` no-op (`{deduped: true}`, no writes, no extractor call), vs F1.1's _global_ `409`, with a superseded doc still re-extracting; goal/audience **re-merge admin-wins-per-field**. **UI:** a **Re-ingest** action on each draft version of the detail page (hidden/disabled for launched/archived) opening `reingest-dialog.tsx` — file picker + optional goal/audience + extract-tables toggle (`FieldHelp` on each) behind a destructive confirm; an identical upload reports "nothing changed". **Tests:** 28 route integration (`reingest-routes.test.ts`: gate order, 401/403, 404s, 409 non-draft, size/format guards, parse/scanned/empty 422, dedup short-circuit, happy path, dispatch-error 429/502/503, rate-limit, audit) + 8 writer unit (`reingest.test.ts`); full questionnaire suite green; `npm run validate` clean. **Docs:** new `.context/app/questionnaire/reingest.md` + cross-refs from `ingestion.md` and `admin-ui.md`/`README.md`. **No CHANGELOG entry** (app surface, not Sunrise platform). **Merging this completes P2.** Tracker: `planning/features/f2.4.md`. **Gates all green:** `/pre-pr`, `/security-review`, `/code-review high`, `/test-review` — findings fixed and amended into the single commit (active-source-scoped dedup, override-not-dropped, in-transaction draft re-check/TOCTOU, applied-only change counts, real-class `instanceof` mock wiring).
- **2026-06-04 — F2.3 extraction-change review + revert ([PR #24](https://github.com/human-centric-engineering/conquest/pull/24)).** The third P2 feature and the **admin trust surface over P1's editorial change log**: list every `AppQuestionnaireExtractionChange` the extractor recorded during ingestion (F1.1) — source quote, before/after, rationale — and **revert** any one, behind `APP_QUESTIONNAIRES_ENABLED` (404 when off), riding the F2.1 mutation pipeline (incl. version-fork-on-launched). **No migration** — `status`/`revertedAt`/`revertedByUserId` already exist from F1.1. Shipped across **2 PRs** (read surface, then the high-stakes revert engine — own review pass). **Pure planner** (`lib/app/questionnaire/extraction-review/planner.ts`, Prisma-free): `planRevert(change, snapshot) → RevertPlan | RevertImpossible` with typed `RevertImpossibleReason` (`target_not_found | ambiguous_target | missing_before_json | structural_inverse_unavailable | graph_drift`); semantics by confidence — **faithful** (`infer_goal`/`infer_audience` clear the inferred field(s) with a drift guard, `prune_*` re-create from `beforeJson`), **best-effort-fails-cleanly** (`rewrite_prompt`/`correct_*`/`infer_type`/`augment_question` reconcile the null-`targetEntityId` entity against the live graph, tie-broken on `sourceQuote`; zero/≥2 → `target_not_found`/`ambiguous_target`), **structural-default-fail** (`merge_questions`/`split_question`/`add_section` only when `beforeJson` reconstructs and all products reconcile uniquely, else refuse — never a half-restored graph). **Decision: full revert across all change types**, not a list-only or tractable-types-only surface. **APIs** (`…/versions/[vid]/changes/…`, wrapper `withQuestionnairesEnabled(withAdminAuth(...))`, registered as `versionChanges`/`versionChangeRevert`): `GET …/changes` (newest-first; filters `status`/`changeType`/`targetEntityType`; each row enriched with a dry-run verdict — `revertable`/`revertBlockedReason`/`revertSummary`); `POST …/changes/:changeId/revert` with load-bearing order — scope-404 version → scope-404 change → **409** if already reverted → **dry-run the planner before forking** (`RevertImpossible` → **422 `REVERT_IMPOSSIBLE`**, no orphan draft) → fork a launched version → apply the inverse to the editable version → **mark the source version's change row `reverted`** (forks start a clean lineage and don't copy change records; UI redirects to the draft with a lineage banner), one transaction, audited `questionnaire_change.revert`; the common just-ingested-`draft` path flips the row in place with no fork. **UI:** dedicated sub-route `app/admin/questionnaires/[id]/extraction-changes?v=<versionId>` (server component, flag-gate + `serverFetch`) + `extraction-changes-table.tsx` (grouped by change family, client-side filters, before/after JSON, per-applied-row revert via shared `authoringMutate` with confirm dialog and disabled+tooltip when the server verdict says it can't revert cleanly, fork-redirect/`router.refresh()`); the detail page links in via the per-version `changeCount`. **Decision: UI is a sub-route, not a detail-page tab.** **Tests:** full `planner` unit suite (every type incl. impossible/ambiguous/drift, `sourceQuote` tiebreak, JsonNull audience clear) + schema unit + integration (gate/scope-404, list, filters, each revert path, re-revert 409, 422-before-fork, fork remap, audit; new-file coverage — planner branches 87%, executor branches 88%/lines 100%, schemas 100%). `npm run validate` clean; `npm run test` full suite green (20,263 passing). Docs: `extraction-changes.md` ("Revert (F2.3)" shipped) + `admin-ui.md` (sub-route + endpoints + reconciliation caveat). **No CHANGELOG entry** — fork-owned app surface, not the Sunrise platform public surface. Tracker: `planning/features/f2.3.md`. **Gates all green:** `/pre-pr` (two flagged executor/planner coverage gaps closed), `/test-review` (high-confidence findings applied — envelope assertions, query-filter coverage, `clearAllMocks` implementation-leak fix), `/security-review` (no findings — two-level scoping, admin guard on both routes, every revert-op id sourced from the scoped live snapshot rather than attacker-influenced JSON).
- **2026-06-03 — F2.5.1 Demo-client foundation (shipped — [PR #21](https://github.com/human-centric-engineering/conquest/pull/21); P2.5 complete).** The whole of the decomposed P2.5: demo-client identity + attribution, behind `APP_QUESTIONNAIRES_ENABLED` (off). **Schema:** `AppDemoClient` (`app_demo_client` — `slug @unique`, `name`, `description?`, `isActive`; identity-only, theme columns deferred to F3.4/F7.1) + nullable `demoClientId` FK on `AppQuestionnaire` with `onDelete: SetNull` + reverse `questionnaires` relation + `@@index`. Migration `…_app_demo_client` created `--create-only`, schema-fold phantoms (3 pgvector DROP INDEX + `searchVector` ALTER) stripped by hand, applied via `db:migrate:deploy`; drift-check 9/9 green. **Domain** (`lib/app/questionnaire/demo-clients/**`, `// DEMO-ONLY:`, Prisma-free): client-safe view contracts, kebab-case `slugifyDemoClient` (+ pattern), create/update/assign Zod schemas. **API** (`app/api/v1/app/demo-clients/**`): `GET`/`POST` (root) + `GET`/`PATCH`/`DELETE` (`[id]`) — all flag-gate→`withAdminAuth`→404→Zod→audit; slug derive-with-override; `409 SLUG_CONFLICT` (P2002); **`409 DEMO_CLIENT_IN_USE` delete-guard** (refuse while `_count.questionnaires>0`); read model `_lib/read.ts`. **Attribution:** `PATCH /questionnaires/:id { demoClientId }` (the one shared-file touch with John — additive handler; `404 DEMO_CLIENT_NOT_FOUND` on attach to a missing client) + `demoClient` surfaced on the list/detail read models + `QuestionnaireListItem`/`QuestionnaireDetail`. Registry `API.APP.DEMO_CLIENTS`; nav entry appended to `initAppNav()` (seam 4). **UI:** `/admin/demo-clients` list + `/new` create + `[id]` edit/delete (`<DemoClientForm>`, `<DemoClientsTable>`, `<DemoClientActions>` with the in-use-disabled delete + alert-dialog confirm), and `<DemoClientAssign>` picker embedded in the questionnaire detail page. **Tests:** schema-shape (model + FK/onDelete/index + footgun guard), slug + schema units, read-model unit, demo-client route integration (gate order, 401/403, 404, slug 409, delete 409-guard, audit), attribution route integration (404s, attach/detach, audit). `npm run validate` green; full suite 20018 pass (the 8 "errors" are pre-existing vitest worker-pool startup timeouts — the named files pass in isolation). Docs: `questionnaire/demo-clients.md` + `schema.md`. No CHANGELOG entry (app surface, not Sunrise platform). Tracker: `planning/features/f2.5.1.md`. Gates all green: `/pre-pr` (one coverage gap found + fixed — `schemas.ts` 50%→100% branch), `/security-review` (clean), `/code-review` (2 cleanups applied — shared `toDemoClientView`/`DEMO_CLIENT_SELECT` + slimmed attribution response to `{ id, demoClient }`), `/test-review` (no findings ≥80).
- **2026-06-02 — F2.2 tagging — built on `feat/f2.2-tagging` (rebased on `main`; gates green).** The second P2 feature: a per-version **tag vocabulary** + **M:N assignment to questions**, behind `APP_QUESTIONNAIRES_ENABLED` (off), single PR. **Schema** (migration `…_app_question_tags`): `AppQuestionTag` (version-scoped; `label` + `normalizedLabel` case-insensitive dedup key + optional `color` from the `TAG_COLORS` allowlist; `@@unique([versionId, normalizedLabel])`) + `AppQuestionSlotTag` M:N join (`@@unique([questionSlotId, tagId])`); both all-cascade, no `User` FK (UG-1). Migration hand-trimmed of the schema-fold phantom DDL (3 pgvector `DROP INDEX` + `searchVector` ALTER) and applied via `migrate deploy` to dodge the mid-apply footgun; drift-check 9/9 green. **Pure core** (`lib/app/questionnaire/tagging/`, Prisma-free): `normalizeTagLabel` + create/update/set-assignment Zod schemas; `TAG_COLORS` single-source tuple in `types.ts`. **Fork seam:** the F2.1 `// F2.2:` placeholder in `_lib/fork.ts` now copies the vocabulary into the forked draft and re-links assignments through the question id-map; `ForkResult.tagIdMap` added so the post-fork assignment route remaps client-sent ids. **APIs** (`_lib/tagging-routes.ts`): `POST`/`PATCH`/`DELETE …/versions/:vid/tags[/:tagId]` (vocabulary) + `PUT …/versions/:vid/questions/:questionId/tags` (replace-set, idempotent). All on the F2.1 pipeline (flag-gate → `withAdminAuth` → scope-404 → `forkVersionIfLaunched` → validate → tx → P2002→400 → `logAdminAction` → `successResponse(data, {forked})`). The **same-version invariant is application-layer** — the assignment route validates every tag id against the question's version (`resolveAssignableTags`, which returns the validated rows so the response needs no readback) **before** forking, so a cross-version id is a 400 with no orphan draft; a validated tag that fails to remap after a fork (concurrent delete) surfaces as a 409, not a silent partial assignment. The **vocabulary create/rename routes pre-check the label too** (`assertTagLabelAvailable`, mirroring `assertKeyAvailable`) **before** forking, so a duplicate-label collision on a launched version is a 400 with no orphan draft (P2002→400 stays as the write-race backstop). Audited as `questionnaire_tag.{create,update,delete,assign}`. **Read:** `getVersionGraph` loads the version vocabulary + per-question tags in the same single nested query (no N+1); `TagView` + `tags` added to `QuestionSlotView`/`VersionGraphView`. **UI:** `tag-vocabulary-editor.tsx` (CRUD), `question-tags-editor.tsx` (popover checkbox multiselect → replace-set `PUT`), `tag-chip.tsx` (shared coloured pill, server + client), read-only chips on `version-graph.tsx`; all through the existing `authoringMutate`/`run` runner (fork notice + refetch). **Decisions:** replace-set `PUT` over per-link; label + optional colour (allowlist, semantic names not hex). **Tests:** schema-shape (2 new tables — DMMF + migration SQL: uniques/indexes/FK-cascade + phantom-strip guard), unit (`normalizeTagLabel`, schemas), fork (`fork.test.ts` extended — vocabulary copy + assignment re-link), route integration (`tagging-routes.test.ts` — gate order, scope-404, dedup 400 **pre-fork + write-race backstop**, **cross-version 400 pre-fork**, **409 on lost-after-fork tag**, replace/clear semantics, fork remap, audit). `npm run validate` + full questionnaire suite (364) green. Docs: `questionnaire/schema.md` + `admin-ui.md`. No CHANGELOG entry (app surface, not Sunrise platform). Tracker: `planning/features/f2.2.md`. **Gates all green** (all fixes amended into the single commit): `/pre-pr` (format nit + `[tagId]`-route branch-coverage gap 68→88% fixed), `/test-review` (6 findings ≥80 applied — auth-matrix gaps, missing status asserts, version-404, pre-fork assertion, non-P2002→500, `status:'applied'` filter pin), `/security-review` (clean — IDOR scoping, same-version invariant, fork remap, XSS, authz all verified), `/code-review high` (7 findings applied — the **orphan-draft class**: pre-fork label check on create/rename + colour-`Select` `disabled={busy}`; `normalizedLabel` dropped from the rename audit diff; `resolveAssignableTags` returns rows so the assignment response skips a readback and a lost-after-fork tag 409s; optimistic-ref fix for the rapid-toggle checkbox race).
- **2026-06-02 — F2.1 PR2 questionnaire structural authoring + version-fork ([PR #19](https://github.com/human-centric-engineering/conquest/pull/19)).** The write surface of the P2 keystone (PR1 #18 shipped the read surface): an admin can now author a questionnaire — edit goal/audience, add/edit/remove/reorder sections & questions, move questions across sections — with the **version-fork-on-launched** lifecycle live behind `APP_QUESTIONNAIRES_ENABLED`. **No schema change** (every needed column already exists). **Pure core** (`lib/app/questionnaire/authoring/`, Prisma-free): `launch-blockers.ts` (`countLaunchBlockers(versionId)` → `{sessions:0, invitations:0}` P3/P4 seam + `hasLaunchBlockers`); `key.ts` (`slugifyKey` + `nextAvailableKey` per-version key derivation + collision suffixing); `type-config-schema.ts` (per-type `typeConfig` Zod + `validateTypeConfig` + `defaultTypeConfig` — choices ≥2 distinct · likert max>min · numeric bounds · config-less types); `schemas.ts` (request bodies `updateVersionMeta`/`updateVersionStatus`/section+question create/update/`reorder`; enums derive from `types.ts` tuples, audience reused from ingestion, provenance server-stamped not client-sent). **Fork seam** (route-local `_lib/`): `fork.ts` — `forkVersionIfLaunched` deep-copies goal/audience/provenance + section→question graph into a new `draft` when the target is launched (or a blocker is live), preserving ordinal/key/typeConfig and returning old→new id maps so child mutations retarget the copy; tags **not** copied (F2.2 seam), change records **not** copied (clean editorial lineage), audited as `questionnaire_version.fork`; `authoring-routes.ts` holds shared glue (`loadScopedVersion` scope-404, `resolveForkedId`, `forkMeta`, `applyReorder` permutation guard, `asKeyConflict`/`assertKeyAvailable` P2002→400, `resolveQuestionKey`, change-aware `adminAudienceProvenance`, `jsonInput`). **APIs** (under `API.APP.QUESTIONNAIRES`, each: flag-gate via `withQuestionnairesEnabled` → `withAdminAuth` → scope-404 → fork preamble → Zod validate → tx → P2002→400 → `logAdminAction` → `successResponse(data, {forked,…})`): `PATCH …/versions/:vid` (meta) · `PATCH …/versions/:vid/status` (`draft ↔ launched`, `→ archived`, with minimal launch guard: goal + ≥1 section + ≥1 question) · section `POST`/`PATCH`/`DELETE`/`reorder` · question `POST`/`PATCH` (incl. cross-section move)/`DELETE`/`reorder`. **UI**: `?edit=1` over the PR1 detail view — `version-editor.tsx` + `section-editor.tsx` + `question-editor.tsx` (+ `version-editor-types.ts`) with `@dnd-kit` drag-reorder (added as an app dependency), inline edits, per-type config editors, `<FieldHelp>` on goal/audience, status controls, `router.refresh()` refetch, and the "edited a launched version → new draft (vN)" fork notice + redirect; meta-aware client helper `authoring-mutate.ts` exposes the fork `meta` that `apiClient` discards. **Decisions**: reorder = drag-and-drop (`@dnd-kit`); manual edits are `logAdminAction`-only (do **not** write `AppQuestionnaireExtractionChange` rows — that log stays the extractor's F2.3 provenance); status transitions pulled forward from P3 to make the fork seam live end-to-end (full config-completeness launch gate + invitations/cost stay F3.1/P3); delete cascades with UI confirm. **Tests**: questionnaire suite **302** green (PR1 was 178, +124) — fork decision matrix (`fork.test.ts`), key/typeConfig/schema/launch-blocker units, change-aware provenance, route integration (gate order, scope-404, fork retarget incl. stale-id 404, status legality + launch guard, typeConfig/key 400s, move/reorder, audit emission); changed route/lib/pure files all ≥80% coverage; `npm run validate` clean. **Gates**: `/security-review` clean (IDOR/scoping, injection, validation, data-exposure verified); `/code-review` (high) surfaced 8 correctness items + cleanups all fixed in-branch (provenance flips to `admin-supplied` only for changed fields; same-section move ordinal excludes the moving question; local pre-validation of likert/numeric/choice editors; forked edits hold the busy lock until the refreshed version loads to avoid duplicate orphan drafts; explicit-key collisions pre-checked before forking; type changes reset config; single version-row read on the fork path). No CHANGELOG entry — app surface, not Sunrise platform public surface (consistent with PR1). Tracker: `planning/features/f2.1.md`.
- **2026-06-02 — F2.1 authoring PR1 (read surface + admin shell) — on `feat/f2.1-pr1-read-surface`, P2 begins.** The first admin UI for the platform, read-only, behind `APP_QUESTIONNAIRES_ENABLED` (off). Three admin GET endpoints (`app/api/v1/app/questionnaires/`): `GET /` (paginated enriched list — latest-version + section/question counts in a fixed 4-query budget, no per-row N+1; query `page/limit/q/status/sortBy/sortOrder`; read model `_lib/list.ts`), `GET /:id` (questionnaire + newest-first version summaries; `_lib/detail.ts`), `GET /:id/versions/:vid` (full section→question graph, scoped by both ids → mismatch 404s). All: flag-gate first (404 off) → `withAdminAuth` (401/403) → 404 unknown id. View contracts (client-safe, ISO dates) in `lib/app/questionnaire/views.ts`; endpoints under `API.APP.QUESTIONNAIRES`. **Provenance stored per field (not derived):** new `goalProvenance` + `audienceProvenance` columns on `app_questionnaire_version` (migration `…_app_questionnaire_version_provenance`, phantom DDL stripped, drift-check green) persist the ingest merge's admin-wins-per-field outcome; the version-graph endpoint reads them straight back and the UI marks each inferred field (per-audience-field, not a coarse flag). An initial read-time derivation from `infer_*` change records was replaced by these columns during PR1 review — the right data model. **Drift fix:** status vocabulary is now the single-source `APP_QUESTIONNAIRE_STATUSES` tuple in `types.ts` (type + Zod query enum + UI filter options all derive from it); `FieldProvenance` moved from route `_lib/merge.ts` into domain `types.ts` so the client-safe view layer doesn't import route internals. UI: nav entry via `registerNavSection()` (seam 4, no sidebar edit); list page (`QuestionnairesTable` — debounced search, status filter, pagination, row click-through) + read-only detail page (`VersionGraph` — SSR `?v=` version selector, goal/audience `inferred` badges, section/question tree); pages 404 via `isQuestionnairesEnabled()` when the flag is off. New tests (route integration: gate order, 401/403, 404, envelope, query parsing; unit: `listQuestionnaires` fixed-query-budget + mapping, `getQuestionnaireDetail`/`getVersionGraph` column-backed provenance + null/404 + version scoping; persist: provenance written; schema-shape: new columns + phantom-strip guard). `npm run validate` + full questionnaire suite (17 files / 178) green. Docs: `questionnaire/admin-ui.md` + `schema.md`. No CHANGELOG entry (app surface, not Sunrise platform). Tracker: `planning/features/f2.1.md`. Remaining in F2.1: PR2 (structural authoring + version-fork). Gates (`/pre-pr`, `/test-review`, `/security-review`, `/code-review`) run next.
- **2026-06-01 — F1.1 ingestion PR4 ([PR #15](https://github.com/human-centric-engineering/conquest/pull/15), open) — F1.1 feature-complete.** The ingestion HTTP endpoint + transactional persistence, behind `APP_QUESTIONNAIRES_ENABLED` (off). `POST /api/v1/app/questionnaires` (`app/api/v1/app/questionnaires/{route.ts,_lib/**}`): flag-gate → `withAdminAuth` → per-admin sub-cap (`ingestLimiter`, 10/min, in-handler — chosen over `registerAppRateLimits`) → `Content-Length`/size guards (25 MB) → `.pdf/.docx/.md/.txt` allowlist → admin goal/audience parse (Zod, empty-as-absent) → SHA-256 dedup (`409`, surfaces existing ids; re-ingest is F2.4) → `parseDocument` direct (no preview/embed) → scanned/empty detection → `capabilityDispatcher.dispatch` of the PR3 extractor (agent binding in `entityContext`, dispatch errors mapped 429/502/503) → coherence pre-check (`assertPersistable` → `422` before any write) → one `executeTransaction` writing the whole graph + change log + source doc → `logAdminAction('questionnaire.ingest')`. Persistence (`_lib/persist.ts`) applies the **admin-wins-per-field** goal/audience merge (`_lib/merge.ts`, pure) and returns per-field provenance; `targetEntityId` resolves to the version for `infer_*`, null for entity edits; raw `bytes` deliberately not stored in F1.1 (no consumer until F2.4 — open question #5). Capability stays Prisma-free; this `_lib/` is the only DB seam. 49 new tests (22 route integration covering 404/401/403/201 + 6 failure-mode envelopes + dedup + dispatch-error mapping + rate-limit + audit; 27 unit on merge/persist/upload-input); `npm run validate` + full questionnaire suite (118) green. Docs: `questionnaire/ingestion.md` + `extraction-changes.md`; synthetic `tests/fixtures/app/questionnaire/` (typo + boilerplate + compound + duplicate). No CHANGELOG entry (app surface, not Sunrise platform). Tracker: `planning/features/f1.1.md`.
- **2026-06-01 — F1.1 ingestion PR3 ([PR #14](https://github.com/human-centric-engineering/conquest/pull/14), open).** The extractor **capability** + its agent/capability seeds + registration, behind `APP_QUESTIONNAIRES_ENABLED` (off). `AppExtractQuestionnaireStructureCapability` (`lib/app/questionnaire/capabilities/**`) — a `BaseCapability` dispatched **programmatically** (not an LLM tool loop): resolves a provider-agnostic binding (read from dispatch `entityContext`, empty-binding → system default), runs `runStructuredCompletion` (parse → retry-once-at-temp-0 → cost-sum), validates against the PR2 Zod contract, normalises the change log, logs `CostOperation.CHAT` cost (fire-and-forget, isolated), and returns structure + changes. **No persistence** (PR4 owns the transactional write); stays Prisma-free per the `lib/app/**` ESLint boundary. `processesPii: true` + `redactProvenance()` (document text → sentinel, counts-only ≤200-char preview); **no silent fallback** on final parse failure — surfaces a typed error carrying the Zod issue paths. Seeds `002-extractor-agent` (empty model/provider, budget cap, `internal`, KB-restricted) + `003-extraction-capability` (`internal`, bound to the agent), idempotent; registered via the `initAppCapabilities()` seam. 23-case dispatch integration test through the real dispatcher + real `runStructuredCompletion`, provider mocked; capability coverage 100% stmts/funcs, 93% branches. Gates run on-branch: `/pre-pr`, `/test-review` (no findings ≥80; 2 app-tier near-misses fixed), `/security-review` (clean — real upload/auth surface lands in PR4), `/code-review high` (3 findings: `functionDefinition` dedup + Zod-issue surfacing fixed; agent sampling-param threading deferred until extraction is exercised on real docs). Two items routed upstream rather than forked: a `runStructuredCompletion` dynamic-retry-message enhancement and a `registry.test.ts` handler-reset. Only PR4 (route + persistence) remains. Tracker: `planning/features/f1.1.md`.
- **2026-06-01 — F1.1 ingestion PR1 + PR2 ([PR #13](https://github.com/human-centric-engineering/conquest/pull/13), open).** The data foundation + pure extraction core for questionnaire ingestion, behind `APP_QUESTIONNAIRES_ENABLED` (off). **PR1 (schema):** the ingestion graph on the F0.1 anchor — `AppQuestionnaireSection`, `AppQuestionSlot` (per-version unique `key`, `type`/`typeConfig`/`weight`, embedding deferred to F4.1), `AppQuestionnaireExtractionChange` (revertible audit trail), `AppQuestionnaireSourceDocument`, plus `goal`/`audience` on the version; no `User` FK (UG-1), all-cascade. Migration hand-trimmed of the schema-fold phantoms — and a Prisma-7 lesson banked: `migrate dev` is **not** transactional, so the phantom index DROPs partially committed mid-apply (recovered surgically, recipe in `questionnaire/schema.md`; drift-check 9/9). **PR2 (pure core):** `lib/app/questionnaire/ingestion/**` — the Zod extraction contract + `z.toJSONSchema` + `validateExtraction`, the provider-agnostic prompt builder, and the change-record normaliser (per-type coherence + admin-wins-per-field suppression); pure, DB-free, framework-free; 44 zero-mock unit tests, 100% coverage on changed files. PR3 (capability) + PR4 (route + persistence) remain. Gates run on-branch: `/pre-pr`, `/test-review` (3 fixed), `/security-review` (clean), `/code-review` (4 fixed). Tracker: `planning/features/f1.1.md`.

- **2026-06-01 — Upstream sync: UG-1 + UG-2 resolved.** Second `upstream/main` merge, pulling down the two F0.1-surfaced fixes Conquest raised: **#283** (UG-2 — pin `AiConversation` inbound unique DB name with `map:`, killing the phantom `ALTER INDEX … RENAME` on every `migrate dev`) and **#284 → #286** (UG-1 — app-extensible drift-probe seam: `lib/db/drift-probes.ts` + the `lib/app/db-drift.ts` `registerAppDriftProbes()` hook, so apps register unmodelled DB objects without forking `scripts/db/check-drift.ts`). Clean merge — no conflicts; Conquest had not modified any of the touched files. The #283 baseline edit is comment-only (DDL unchanged), so no new migration; existing dev DBs needed a one-row `_prisma_migrations.checksum` realignment (done locally — see UG-2 resolution note). Both gaps retired in `planning/upstream-gaps.md`.

- **2026-06-01 — F0.1 Foundation scaffolding (shipped — [PR #10](https://github.com/human-centric-engineering/conquest/pull/10), merged).** App docs consolidated under `.context/app/` (`planning/` + `questionnaire/` namespaces). Questionnaire module skeleton + DB-backed `APP_QUESTIONNAIRES_ENABLED` flag (seeded off) with the `ensureQuestionnairesEnabled()` route-gate template; app-owned Prisma schema (`AppQuestionnaire` + `AppQuestionnaireVersion`, app-internal cascade relation, User FK deferred — see `planning/upstream-gaps.md` UG-1) + init migration hand-trimmed of the platform schema-fold DROPs (drift-check green); recursive app seed for the flag; gated `GET /api/v1/app/healthcheck` (404 off / 200 on). Unit + integration tests; `validate` + full `test` green. Six PR-sized tasks (one commit each) batched into one PR — tracker: `planning/features/f0.1.md`.

---

## Carried Sunrise patches

Core changes the app currently carries that are not yet reflected in upstream Sunrise. Each tagged `pending-upstream | app-specific-override | abandoned`. Retired when a Sunrise release includes the change.

- **2026-05-31 — CI private-fork fixes + performance — RETIRED (landed upstream).** Was `pending-upstream`; now superseded by Sunrise #280 ("ci: adaptive pipeline for public + private forks, with perf overhaul") and pulled down in the first `upstream/main` merge on 2026-05-31. All Conquest carried CI overrides (heap bumps in `ci.yml`/`Dockerfile`, CodeQL + dependency-review private-gating, Tier-1 caching/concurrency/`--changed`, Tier-2 parallel+sharding) were resolved **in upstream's favour** during the merge — upstream's version is a superset (adaptive public/private, API-based visibility check that fixes the CodeQL `schedule` edge we'd flagged, `CI_TEST_SCOPE` knob). No Conquest-specific CI override remains; canonical behaviour now documented in `.context/architecture/ci.md`. The seed-credential issue (#278) also landed (first-user-is-admin + `system-owner` seed) — not a carried patch, but the README "first admin" section was updated to match during the merge.

---

## References

- [[building-on-sunrise]] — canonical model for how apps relate to Sunrise.
- [[fork-readiness-backlog]] — the pre-fork Sunrise work, now closed in v0.0.1.
- [[Conversational Questionnaire Phases]] — the original prompt-per-phase document. Superseded as the working plan, kept as deep-spec reference per phase.
- [[v1-requirements|HCE Hub v1 requirements]] — the working model this doc mirrors (Project → Feature → Task, ownership, promotion).
